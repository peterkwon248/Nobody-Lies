# 기본 디자인 감사 — 2026-08-05

> ## ✅ 수리-A 재측정 (2026-08-06)
>
> **#1 · #2 · #7 · ★ 넷이 닫혔다.** 같은 스크립트를 다시 돌린 실측이다.
>
> ```
>                        전 (08-05)        후 (08-06)
> 터치 타깃 둘 다 <44   mobile   72          0
>                       tablet   72          0
>                       laptop   98          0
>                       desktop  98          0
> 캡처 건수                      52          56
> 못 잰 것                        4           0    log-{empty,mid}-{mobile,tablet}
> ```
>
> ★ **#1 은 감사 도구가 스스로 증명했다** — `goto()` 는 사이드바 → 하단탭 →
> 더보기 시트 순으로 훑는데, 좁은 폭에서 `조사 기록` 을 **셋 다에서 못 찾아**
> 건너뛰고 있었다. 시트에 넣자 같은 코드가 찾아서 눌렀고 화면 수가 12 → 14 가 됐다.
> **내가 「고쳤다」고 말한 게 아니라 도구가 닿게 된 것이다.**
>
> ⚠ **아래 §도구가 자기를 세 번 속였다 의 셋째를 먼저 읽어라** — 이번에 나온 것이고
> **표의 화면별 행 전체에 걸린다.**

> **측정만이다. 수리안도 우선순위도 없다.** (`docs/DESIGN-AUDIT-SPEC.md` §2·§3)
> 심각도는 기계적 기준으로만 달았다 — **무엇을 먼저 고칠지는 사용자가 찍는다.**
>
> 재현: `npm run dev` 를 띄우고
> ```
> node scripts/design-audit.mjs      # 4폭 캡처 + computed style 크롤
> node scripts/token-audit.mjs       # 축5 정적 전수
> ```
> 산출: `docs/audit-data.json` · `docs/audit-tokens.json` · `docs/audit-shots/*.png`
> **수리 후 같은 스크립트로 다시 재는 것이 진척의 실측이다.**

## 측정 범위

```
뷰포트    375×812 · 768×1024 · 1280×800 · 1536×960          (지시서 §0 그대로)
캡처      52건 → 56건 (08-06)   화면 12~14 → 14 × 4폭 (상태: 빈 · 진행 중 · 완료)
못 잰 것   4건 → 0건 (08-06)    log-{empty,mid}-{mobile,tablet}
                 ↑ 도구 실패가 아니라 **닿을 수 없어서**였다 (#1 · 닫힘)
```

> ⛔ **캡처 56장이 이름과 다른 화면이다** — 아래 §도구가 자기를 세 번 속였다 셋째.
> 숫자(56 · 0)는 참이지만 **PNG 는 전부 홈 덮개**다. 아직 안 고쳤다.

---

## 요약 수치 — 수리 후 다시 잰다

```
타이포 조합(상위 합집합)   33종     4폭에서 **완전히 동일** (반응 없음)
14px 미만 타이포 조합      41종     최다 `12px/500/15` 495회
하드코딩 hex              100건    고유 18색 · Generator 76 · App 23 · main 1
rgba() 리터럴              33건
strokeWidth 값             12종     1 · 1.2 · 1.3 · 1.4 · 1.5 · 1.6 · 1.75 · 1.8 · 3 · 4.5 · 7 · 10  (80회)
간격 4px 그리드 밖        244건    3 5 6 7 9 10 11 13 14 15 18 22 26 34
breakpoint                  1개    JS `< 820` 하나. **CSS `@media` 0건**
인라인 스타일             713회    `S()` 호출 · 요소의 57~73% 가 style 속성 보유
터치 타깃 둘 다 <44px   72 / 115    모바일 (63%)  → ✅ **0 / 4폭 전부** (08-06 · #2)
CSS 상태 선택자        hover 97 · focus-visible **0** · focus 3 · disabled **0** · active 4
transition 조합             5종     animation 요소 **0** · `prefers-reduced-motion` **0건**
```

---

## 표

| # | 축 | 화면(캡처 키) | 뷰포트 | 실측 | 심각도 |
|---|---|---|---|---|---|
| 1 | 3 | `log-*` | mobile · tablet | ~~**조사 기록 화면에 도달 경로가 없다.** `buildBottomNav`(narrative·statements·map·profile·더보기)에도 `buildMoreNav`(overview·memo·graph·reference)에도 `log` 가 없다. 사이드바는 `isNarrow` 에서 숨는다 → 좁은 폭에서 조사 기록은 **열 수 없다**~~ → ✅ **닫혔다 (08-06)** — `MORE_ITEMS()` 맨 앞에 `log` 를 넣었다. 캡처 12 → 14화면 | ~~깨짐~~ |
| 2 | 3 | 전 화면 | mobile · tablet | ~~인터랙티브 115개 중 **72개가 가로·세로 둘 다 44px 미만**(63%). 상단 크롬이 전부 여기 있다 — `뒤로` `나란히 보기` `설정` 26×26 · 설정 28×28 · `EN` 38×24 · 테마 `◑` 29×24~~ → ✅ **닫혔다 (08-06)** — `styles.css` 에 `.iconbtn,.seg{min-width:44px;min-height:44px}` 한 줄. 4폭 전부 **0**. 상단 바 36 → 44px, 가로 넘침 0 | ~~깨짐~~ |
| 3 | 3 | 전 화면 | 4폭 | **`:focus-visible` 규칙 0건.** hover 는 97개인데 키보드 초점은 어디에도 안 그려진다. `press()` 로 `role=button`·`tabIndex=0` 을 붙여 **탭이 가긴 가는데 어디 있는지 안 보인다** | 깨짐 |
| 4 | 1 | 전 화면 | 4폭 | **타이포가 뷰포트에 반응하지 않는다.** 상위 조합 33종이 375와 1536에서 **완전히 같다.** 반응하는 것은 `--read-*` clamp 를 쓰는 읽는 화면 여섯뿐(2026-08-05 ②에서 넣은 것) | 어긋남 |
| 5 | 1 | 전 화면 | 4폭 | **14px 미만 조합 41종.** 최다 `12/500/15`(495회 · "공란을 모두 채우면…"), `12/600/normal`(456회 · 난이도 칩), `11/500/13.75`(370회 · 소요 시간), `11/600`(232회 · 상태 칩) | 어긋남 |
| 6 | 1 | 전 화면 | 4폭 | 조합 수가 화면당 259(모바일)~322(데스크톱)로 **역할 고정 4~5단의 목표에서 멀다.** 같은 역할(카드 제목)이 `13/500/16.25` 와 `13/500/normal` 로 갈린다 | 어긋남 |
| 7 | 3 | 하단 `더 보기` 칩 | mobile · tablet | ~~활성 판정 목록이 `['overview','memo','map','reference']` 인데 **`map` 은 직접 탭이고 `graph` 는 시트 안**이다. 현장에 있으면 칩이 잘못 켜지고, 관계도에 있으면 안 켜진다 (`App.jsx:1872`)~~ → ✅ **닫혔다 (08-06)** — 목록을 **지웠다.** 시트 내용과 활성 판정이 `MORE_ITEMS()` 하나에서 난다. 브라우저 실측: 조사 기록에서 켜지고(accent) 현장에서 꺼진다(`--fg-3`) | ~~어긋남~~ |
| 8 | 5 | `Generator.jsx` | — | **하드코딩 hex 76건.** 이 파일 하나가 전체의 76%다 | 어긋남 |
| 9 | 5 | `App.jsx` | — | 하드코딩 hex 23건 + rgba() 리터럴 33건. **`--game-*` 폴백을 끼울 토큰 자리가 없는 지점들**(스킨 지시서 §7이 예고한 목록) | 어긋남 |
| 10 | 5 | 전 파일 | — | **strokeWidth 12종 80회.** 1 · 1.2 · 1.3 · 1.4 · 1.5 · 1.6 · 1.75 · 1.8 · 3 · 4.5 · 7 · 10 — 아이콘 획이 화면마다 다르다 | 어긋남 |
| 11 | 5 | 전 파일 | — | **간격 4px 그리드 밖 244건.** 3 5 6 7 9 10 11 13 14 15 18 22 26 34 | 어긋남 |
| 12 | 5 | 전 화면 | 4폭 | 요소의 **57%(데스크톱)~73%(모바일)가 인라인 `style` 보유**, `S()` 호출 713회. 스킨 층이 끼어들 자리가 CSS 가 아니라 JS 문자열 안에 있다 | 어긋남 |
| 13 | 5 | `styles.css` | — | **CSS `@media` 0건.** 반응형 어휘 전체가 JS `isNarrow < 820` 하나다. 「판정이 두 벌이면 갈라진다」는 지켜졌지만 **CSS 쪽 어휘가 아예 없다** | 어긋남 |
| 14 | 3 | 전 화면 | 4폭 | `:disabled` 규칙 **0건**, `:active` 4건. 비활성은 인라인 `opacity`·`pointerEvents` 로 자리마다 따로 표현된다 | 어긋남 |
| 15 | 4 | 전 화면 | 4폭 | **`prefers-reduced-motion` 0건** | 어긋남 |
| 16 | 2 | 전 화면 | mobile 14 · tablet 5 · laptop/desktop 3 | 내부 가로 overflow 요소. **문서 가로 넘침은 4폭 전부 0** — ②clamp·③탭확대 이후 남은 것은 컨테이너 내부다 | 어긋남 |
| 17 | 2 | 전 화면 | mobile 18 · desktop 23 | 패딩 조합 수(화면 평균). **도구 화면과 서사 화면의 밀도 어휘가 갈려 있지 않다** — 같은 패딩 사전을 두 부류가 나눠 쓴다 | 어긋남 |
| 18 | 4 | 전 화면 | 4폭 | **transition 조합 5종이 전부**: `background .3s`(80) · `left,top,opacity .45s/.3s`(40) · `background,border-color,opacity .12s`(28) · `width .14s`(28) · `background,border-color .2s`(2). **animation 요소 0** | 밋밋함 |
| 19 | 4 | 사건의 순간 셋 | 4폭 | 카드 등장 · 단어 확보 · 장 전환에 **전용 모션이 없다.** `gSettle` 키프레임이 `styles.css` 에 있으나 캡처 시점 적용 요소 0 | 밋밋함 |
| 21 | 2 | 홈 `캠페인 생성` 행 | mobile | **제목이 36px 폭으로 눌려 3줄로 깨진다**(「캠페 / 인 생 / 성」 · 실측 36×61). 한 행에 `＋` · 제목 · 설명 110px · 배지 · `새로 만들기` 59px 를 375 안에 다 넣는다. ⛳ **08-06 수리와 무관한 기존 결함이다** — `.iconbtn/.seg` 규칙을 껐다 켜며 A/B 해서 확인했다(양쪽 동일) | 어긋남 |
| 20 | 2 | 중간 공개 3종 | 4폭 | **무게가 갈려 있다** — 장 전환은 `stage:'interlude'` 로 **전체 화면**(`App.jsx:2469`), 추가 단서 공개는 **토스트 4.6초 + 사이드바 점**(`:2470`·`:2451`), 트릭 인상 붕괴는 **전용 렌더 없음**. 프롤로그/에필로그와 같은 문법을 쓰는 것은 **장 전환 하나뿐** | 밋밋함 |

---

## §구조 백로그 — 이번 수리의 범위가 아니다

> 지시서 §2: *"구조 리팩터링이 필요해 보이는 항목은 고치자고 쓰지 말고 여기 모은다
> — 언젠가 올 UI 재작성의 입력이지 이번 수리의 범위가 아니다."*

- ★ ~~**`buildReadCard()` 무가드 — 앱 전체가 죽는다.**~~ → ✅ **닫혔다 (2026-08-06 ·
  사용자가 수리-A 로 예외 승격).** `renderVals` 가 화면과 무관하게 **매번** 부르는데
  내부는 `PEOPLE[readIdx].name` 을 가드 없이 읽고 있었다. `readIdx` 는 **저장되는
  값**이고 `PEOPLE` 은 **사건 파일에서 온다** — 둘이 어긋나면 「렌더 실패」로 앱이
  통째로 멈춘다. 감사 중 심은 값(`readIdx:5`)으로 **실제로 재현했었다.**
  → 이제 `emptyReadCard()` 로 떨어진다. **가드를 호출부가 아니라 `buildReadCard`
  안에 뒀다** — 호출부에 걸면 다음 호출부가 생길 때 같은 자리에서 다시 죽는다.
- `App.jsx` 4,564줄 단일 클래스 (①에서 −681줄 했으나 여전히).
- 반응형 어휘가 CSS 에 0 — 새 breakpoint 체계는 수리 배치의 설계 대상이다.
- 인라인 스타일 713회가 스킨 배치의 실질 선행 조건 (#12).

## 못 잰 것

```
✅ log-{empty,mid}-{mobile,tablet}   도달 불가라 못 쟀다 → 표 #1 이 그 자체였다
                                     2026-08-06에 닿았다 (0건)
```

## 도구가 자기를 세 번 속였다 (기록)

- **터치 타깃을 「하나라도 44 미만」으로 세어 100% 가 나왔다.** 넓고 낮은
  사이드바 행이 전부 걸린 것이라 **손가락이 빗나가는 부류가 아니었다.**
  둘 다 미만 / 낮기만 / 좁기만 으로 갈라 다시 쟀다(72 · 42 · 0).
- **`더보기` 로 좁은 폭 다섯 화면을 못 찾았다.** 실제 라벨이 `더 보기`(사이 공백)다.
  **눈에 같아 보이는 것이 코드에 같지 않다** — 공백을 지우고 비교하도록 고쳤다.

### ⛔ 셋째 — **56장의 캡처가 전부 홈 화면이다** (2026-08-06 · 아직 안 고쳤다)

수리-A 재측정 중에 `log-mid-mobile.png` 를 열었더니 **조사 기록이 아니라 홈**이었다.
`report-mid-mobile` 도 `log-mid-laptop` 도 같았다. 재보고 원인을 확정했다:

```
seed(localStorage) → page.goto(BASE)   앱이 route:'home' 으로 뜬다
goto(page, '조사 기록')                 setView() 를 부른다 — view 는 바뀐다
                                        그런데 **route 는 'home' 그대로**다
홈은 position:fixed · z-index:70 의 덮개고, 게임 셸(.app)은 그 **밑에 깔린 채**다
```

**`elementFromPoint` 로 확인했다** — 셸의 `.iconbtn` 여섯이 전부 `visible:false`
(덮개에 가려짐)이고, 실제로 눌리는 것은 홈 자신의 것 하나뿐이다.

```
스크린샷    ⛔ 이름이 가리키는 화면이 아니다. 56장 전부 홈이다
크롤 수치    ⚠ 화면마다 다르긴 하다 (pads 14~21 · overflow 0~13) — 덮개 **밑에 깔린**
             셸을 재기 때문이다. 즉 **의도한 화면을 재긴 하는데, 그 순간 화면에 없는
             요소**를 재고 홈 덮개의 요소와 **섞어서** 센다
```

⛳ **그래서 「인터랙티브 115개」 같은 합계는 두 층이 섞인 수다.** 화면별 행(#4~#6 ·
#16 · #17)을 **한 화면의 실측으로 읽으면 안 된다.**

> ★ **수리-A 의 72 → 0 은 이 결함과 무관하게 참이다.** 문제의 일곱 개는 두 층 **모두**에
> 있고, 덮개를 걷은 실제 게임 화면에서 **375·1280 둘 다 `bad: 0`** 을 따로 확인했다.
> 반대로 **표의 나머지 행은 이 결함을 걷어내고 다시 읽어야 한다.**

⛳ **처방은 `goto()` 앞에 「이어하기」를 누르는 한 걸음이다** — 홈 덮개를 걷어야
캡처가 이름과 같아진다. **이번 수리에 안 끼웠다** — 재측정의 기준선을 바꾸는 일이라
수리-A 의 전후 비교와 섞이면 무엇이 움직였는지 흐려진다. **다음 감사 앞에 선다.**
