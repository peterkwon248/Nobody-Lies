# 노바디 라이즈 · 프로젝트 메모리

> `before-work`가 세션 시작마다 읽고 `after-work`가 세션 끝마다 갱신하는 파일.
> 작업 기계가 3대이므로 **기계 사이를 넘어가는 상태는 이 파일과 `.omc/worklog/latest.md`뿐이다.**
> 커밋되지 않은 것은 다음 기계에서 존재하지 않는다.

---

## 이게 뭔가

정지된 살인 사건 하나(산장 살인사건)를 플레이어(무명의 수사관)가 **진술 정독 → 제한된 조사 → 보고서 공란 채우기**로 푸는 추리 게임.

계보는 Return of the Obra Dinn → The Case of the Golden Idol. 차이는 **사건을 논리 검증기로 검사해 자동 생성할 수 있다는 것.** v1은 손으로 쓴 산장 사건 1건.

- **앱 이름**: 노바디 라이즈 (Nobody Lies)
- **저장소**: https://github.com/peterkwon248/Nobdy-Lies (public)
- **현재 단계**: 설계 완료 · 프로토타입 완성 · **플레이테스트 이전** · 프로덕션 코드 0줄

핵심 규칙이 곧 제목이다 — *무고한 사람은 거짓말하지 않는다. 다만 자기 비밀은 말하지 않는다.* 그리고 제목 자체가 거짓이다. 다섯 중 하나는 거짓말을 한다.

---

## 절대 규칙 — 새 화면·기능마다 확인할 것

> **게임이 플레이어의 판단에 대해 하는 말은 최종 채점 하나뿐이다. 그 전까지 완전한 침묵.**

이 게임의 상품은 "내가 알아냈다"는 감각이다. 아래를 위반하면 상품이 사라진다.

| 금지 | |
|---|---|
| 실시간 채점 | 장을 완성해도 정답 여부를 알리지 않는다 |
| 모순 경고 | "확보한 물증과 어긋납니다" 없음 |
| 게임이 하는 텍스트 강조 | 볼드·하이라이트·색칠 |
| 유용도 시각 구분 | 결정적 단서와 레드 헤링이 완전히 동일하게 생겨야 함 |
| 프로필의 유죄 판정 | 사실만. `기회 있음 ✓` 금지 |
| 조사 추천·힌트 | 없음 |
| 자동 분석 일체 | 모순 탐지·타임라인 해석 등 |

**이 규칙은 개발 중 일곱 차례 다른 형태로 재발했다** (배너 → 진술 볼드 → 프로필 판정 → 분석 보조 토글 → 모순 경고 → 물증 카드 `뜻` 필드 → 격자 자동 추출). 전부 "친절한 UI의 기본값"에서 나온다. 보통 앱은 중요한 것을 강조해주는 게 미덕이지만 이 게임에서 그것은 정답 누설이다.

부수 규칙 둘:
- **한 번 보여주고 사라지는 텍스트는 버그다.** 토스트는 "어디에 새 것이 생겼다" 신호만. 내용은 해당 화면에 영구히.
- **런타임 LLM은 0이다.** 예외 없음. 빌드 타임 4곳만(진술 산문 / 프롤로그 / 번역 / UGC 누설 검사), 결과는 파일에 고정.

---

## 파일 지도

```
docs/         설계 정본
  design-brief.md          마스터 스펙. 화면·상태·원칙 (1503줄)
  SYSTEM-DECISIONS.md      채점·경쟁·확장·LLM·수익화 결정
  HANDOFF-TO-CODE.md       개발 인계 — 저장소 분리·스키마·구현 범위
  화면 인벤토리 (개발 인계).dc.html   실제 구현 기준 화면·토큰·상태 목록
  MEMORY.md                이 파일

engine/       사건 검증기 (TypeScript). 이식 원본이지 앱 코드가 아니다
  src/types.ts             사건 데이터 스키마 정본
  src/verifier.ts          검증 알고리즘 정본
  src/cases/mountain-lodge.ts   산장 사건 골든 케이스
  src/cli.ts               검증 결과 출력. 실패 시 exit 1
  templates/               사건 저작 서식 + 작법 문서

prototype/    Claude Design 프로토타입 (DC 런타임). 배포 대상 아님
  추리게임.dc.html         메인. 거의 모든 화면·로직 (2468줄)
  추리판.dc.html           상황판 자식 DC
  _ds/                     Vector Design System 번들
  playtest/                단일 파일 export (플레이테스트 배포본)
  PROTOTYPE-NOTES.md       프로토타입 작업 노트 ※ 일부 stale, 아래 참조

design/       아이콘 탐색(32턴, 건메탈 확정) · 비교 스크랩 · 세션 캡처

app/          프로덕션 — 아직 없음
```

---

## 빌드 게이트

```bash
npm run build
```

= `engine` 타입체크 + 산장 사건 검증. **검증 실패 시 exit 1.**

`after-work`가 이걸 커밋 게이트로 쓴다. 지금 단계에서 가장 의미 있는 게이트는 "사건 데이터가 여전히 논리적으로 성립하는가"이므로 이렇게 걸었다. `app/`이 생기면 앱 빌드를 앞에 붙인다.

현재 통과 상태:

```
산장 살인사건 [campaign] 예산 5 · 5장 19공란
유죄 성립: 미야와키 사쿠라 — 유일
최단 2회 · 기대 5회 (밴드 3~8) · 난이도 hard
물증 7 · 정황 9 · 심증 3 · 조사 23개/예산 5 = 4.6배 · decoy 40%
검증 통과
경고 1건: 기대 5회 + 여유 1 > 예산 5 — 의도된 긴장, 사양 결정 대기
```

---

## 문서 신뢰도 — 읽기 전에 반드시 볼 것

설계 문서가 결정이 뒤집힐 때마다 새 절을 덧붙이면서 **옛 절을 지우지 않았다.** 아래 블록은 죽었다. 곧이곧대로 구현하면 위의 절대 규칙을 위반하게 된다.

| 죽은 블록 | 이긴 결정 |
|---|---|
| `design-brief.md` §9.9 "모순 경고만 표시한다" | §4.1 "모순 경고도 **제거**한다" |
| `SYSTEM-DECISIONS.md` §1 모순 경고 + `Blank.contradictedBy` 스키마 | 위와 동일. 스키마 항목 자체가 폐기 |
| `design-brief.md` §9.9 "`장 확인` 버튼은 되돌릴 수 없음을 알려야" | §4.1 "`장 확인` 버튼을 **제거**한다" — 공란 다 채우면 자동 완성 |
| `design-brief.md` §4.1 "미해금 절도 항목은 보여야 한다" + §6 하지말것 "빈칸 라벨까지 가리는 것" | §4.1 "장 잠금 — 순차. **잠긴 장은 번호만 남기고 전부 가린다**" (제목·라벨·유형·개수). `HANDOFF-TO-CODE.md` §5.1이 확정 |
| `design-brief.md` §3 "서술문 절 3개(빈칸 3/4/3)", §4.1 공란 구성 표의 2·3장 순서 | 5장 19공란. 장은 의존성 순으로 재배열됨 (1 도착과 발견 → 2 마지막 정황 → 3 사인과 현장 → 4 감춰진 사건 → 5 범인과 동기) |
| `prototype/PROTOTYPE-NOTES.md` "잠금 없음(모든 항 처음부터 열람·입력)" | **코드가 맞다.** `추리게임.dc.html`의 `secState()`가 순차 잠금을 구현 중 |
| `design-brief.md` §11 "물증 아이콘 세트 v1 2순위" | `HANDOFF-TO-CODE.md` §8.3 "아이콘·사운드는 **연기**. 증폭기를 먼저 만드는 것이 이 단계의 전형적 실패" |

**우선순위: HANDOFF-TO-CODE.md > design-brief.md §4.1 > design-brief.md 나머지 > SYSTEM-DECISIONS.md**

---

## 엔진 ↔ 프로토타입 데이터 불일치 (미해결)

같은 산장 사건이 두 곳에 있는데 어긋난다. **지금 검증기는 실제로 플레이되는 사건을 검증하지 않고 있다.**

1. **4장 공란 답이 다름**

   | 공란 | `engine` | `prototype` | 판정 |
   |---|---|---|---|
   | 협박대상 | 조유리 | 김채원 | **조유리** — `design-brief.md` §4.6 L1 그래프가 "김선생 → 유리(협박), 채원 → 김선생(공범)". 프로토타입이 틀림 |
   | 인물 | 미야와키 사쿠라 | 장원영 | **결정: 4장을 5공란으로 늘려 둘 다 넣는다** (물품·정체·협박대상·인물(원영)·인물(사쿠라)). 검증 부담 거의 없고 심증 부문 빈약 문제도 같이 풀림 |

2. **확보 단어 획득 그래프가 다름** — `engine`은 `Evidence.yieldsTerms`, `prototype`은 `TERM_MAP` 4개 + `REVEALS[s4].terms`. 검증기의 "`discovered` 공란 답이 실제로 획득 가능한가" 검사가 헛돌고 있다.

3. **동의어 충돌** — 별채 수색 시 확보 단어에 `대포폰`과 `별채 대포폰`이 **둘 다** 들어오는데 b19 정답은 `별채 대포폰`뿐. 짧은 이름을 넣은 플레이어가 오답 처리된다. 설계된 레드 헤링이 아니라 이름 충돌. (엔진 쪽엔 `대포폰`이 없음 — 프로토타입에서만 생긴 문제)

4. **`presence → 진술 주장 도출`이 어디에도 구현돼 있지 않음** — `SYSTEM-DECISIONS.md` §7이 "코드가 담당(전부)"에 올린 항목이고 `templates/README.md`가 "무고한 사람은 거짓말하지 않는다를 보장하는 유일한 방법"이라고 못박았는데, 검증기에 도출 함수가 없고 프로토타입은 `CLAIMS`/`CLAIM_LOC`를 손으로 쓴다. 엔진 `presence`는 무고한 4명 전원 `{t0800, main}` 한 줄뿐인 스텁이고 슬롯 어휘도 다르다(엔진 `t0300/t0330/t0400/t0500/t0800` vs 프로토타입 `t0~t3`).

---

## 그 밖의 미해결

| | |
|---|---|
| 플레이테스트 빌드에 상황판이 없음 | `prototype/playtest/`의 단일 파일에 `<dc-import name="추리판">` 호출부만 있고 자식 DC 코드가 없다(`pb-*` 클래스 0건). 하필 `HANDOFF-TO-CODE.md` §9가 측정하려는 항목이 "격자·상황판·관계 그래프를 실제로 쓰는가 — 안 쓰이면 프로덕션에서 제외"다 |
| 상황판 ≠ 브리프의 심증판 | 브리프 §10은 "별도 화면으로 만들지 않는다. 5장 도달 시 자동 전환"인데 프로토타입은 사이드바 독립 뷰로 만들었다. 브리프가 만들지 말라고 한 형태 그대로. 두 물건이 갈라졌다 |
| 소요 시간 미구현 | `buildResult`의 `{ k: '소요 시간', v: '—' }`. 점수 3요소(조사 횟수 + 정확도 + 시간) 중 하나 |
| 조사 예산 하드코딩 | `remaining = 5 - spent`. `Case.budget` 참조 아님 |
| 난이도 여유 0 | 기대 5 = 예산 5. 헛발질 한 번에 클리어 불가. `hard` 의도면 OK, 미결정 |
| 실존 인물명 | 예나·유진·사쿠라·유리·원영·채원. **상업 출시 전 반드시 교체** |
| 목록 모드 잔재 | 브리프 §4.1이 폐기했는데 `buildListBlank`·`buildBank`가 코드에 남아 있음 |
| 캠페인 구성 | 연작 vs 단편집. 첫 5개 집필 전 결정 필요. 현재 추천은 단편집 + 느슨한 연결 |

---

## 다음에 할 일 (순서가 중요)

`HANDOFF-TO-CODE.md` §0.1이 못박은 순서다. **검증되지 않은 설계를 프로덕션 스택으로 옮기면 두 번 만들게 된다.**

```
1. 문서 모순 정리 (위 표를 문서 자체에 대체 표시로 박기)
2. 엔진 ↔ 프로토타입 데이터 동기화 + 재검증
3. 플레이테스트 빌드 수리 (상황판 포함) → export
4. 플레이테스트 4명
5. 설계 보정
6. 프로덕션 구현      ← 여기가 Claude Code의 진짜 시작점
```

**4번이 가장 큰 미검증 가정이다.** 이 게임은 아직 한 번도 (앱으로) 플레이된 적이 없다. 40개 넘는 설계 결정이 미검증이고, 특히 검증기가 예측한 "조사 5회"가 실측과 맞는지 모른다. **그 숫자가 틀리면 난이도 체계 전체가 다시 계산되고 생성기 신뢰도가 무너진다.**

플레이테스트에서 측정할 것: 진행자 없이 재미있는가 / 몇 회에 풀었는가 / 부문별 성향이 갈리는가 / 순차 잠금이 답답한가 긴장인가 / 완전 침묵이 가혹한가 / **격자·상황판·관계 그래프를 실제로 쓰는가** / 어디서 지루한가 / 진술 5개가 머리에 들어오는가.

프로덕션 구현 순서는 `design-brief.md` §7 우선순위 표를 그대로 쓴다.

---

## 프로덕션에서 새로 필요한 것 (프로토타입에 없음)

| | |
|---|---|
| 저장소 3분리 | `Case`(불변) / `PlayerAnnotations`(가변) / `CaseProgress`(가변). 프로토타입은 단일 상태 객체 |
| 이어하기·진행 저장 | `CaseProgress` |
| 사건 파일 외부화 | `Case`가 불변이므로 정적 파일 + CDN. **협동 모드 전까지 서버가 거의 필요 없다** |
| 레이아웃·로직 분리 | 모바일은 반응형 축소가 아니라 재설계. 지금 정하면 공짜, 나중에 고치면 비쌈 |
| 협동 대비 | 조사 결과에 `by: PlayerId`, `shared: boolean`을 미리 넣어둔다 |
| 빌드·배포 | 웹 + Tauri(스팀·데스크톱) |

Vector Design System은 https://github.com/peterkwon248/vector-ui — 본인 저장소라 프로덕션에서 그대로 쓴다. 새 토큰 체계를 만들지 않고 `[data-surface="vector"]` 스코프에만 얹는다.
