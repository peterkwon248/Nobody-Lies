# 🎯 NEXT ACTION

> 다음 세션이 **가장 먼저** 읽는 파일. 컨텍스트·우선순위·미해결의 정본은
> `docs/MEMORY.md`다 (이 저장소는 MEMORY.md 단일 체제 — 별도 CONTEXT/TODO 없음).
> **다른 컴퓨터에서 시작하면 `docs/MEMORY.md`의 ⚠️ 프로토타입 저장소 관계 경고부터 읽을 것.**

- **마지막 갱신**: 2026-07-24
- **마지막 머신**: DESKTOP-JCJTAH8

---

## 다음 즉시 액션 — 다음 액션 순서 **3번 (플레이테스트 빌드 수리)**

엔진 데이터 정합(1~4번)은 끝났다. 막힌 지점은 **DC 툴 split-brain**이다.

### 왜 지금 바로 코드로 못 넘어가나
플레이테스트 export 가 stale 이다. 내 데이터 수정 7건(조유리·대포폰 병합·원영
확보단어·공범 라벨 제거·foundAt)이 **리포에만 있고 DC 저작 툴(마스터)엔 없다.**
DC 툴은 반대로 floorplan GEO 엔진 등 최근 디자인 작업이 리포보다 앞서 있다.

### 첫 스텝 (순서 중요)
```
3a. DC 툴에 prototype/DC-SYNC-CHANGESET.md 의 7건 적용   ← 사용자, DC 툴 필요
    (dc_js_str_replace 워크플로우. 대상 = DC 툴의 추리게임.dc.html)
3b. DC 툴에서 재export (상황판 추리판.dc.html 형제 파일 자동 포함됨)
3c. 재export 를 리포 prototype/playtest/ 에 반영
    → 브라우저(preview_start)로 열어 상황판(board 뷰)이 실제 렌더되는지 확인
    → pb-* 클래스가 형제 파일로 로드되는지 (dc-import name="추리판")
```

### 그다음
```
4. 플레이테스트 4명  ← 이 게임 최대 미검증 가정 ("조사 5회"가 실측과 맞나)
5. 설계 보정
6. 프로덕션 구현     ← Claude Code 진짜 시작점 (design-brief §7 우선순위)
```

---

## 잊지 말 것 — 이번 세션 핵심 결정

1. **엔진이 게임 로직의 정본이다.** 프로토타입은 이식 대상. 데이터 정합은 전부
   엔진 기준으로 맞췄고 `npm run build`(= 사건 논리 검증) 통과.
2. **DC 툴 = 프로토타입 마스터.** 리포 `추리게임.dc.html`은 낡은 미러.
   프로토타입을 리포에서 직접 편집하지 말 것(고아가 됨). DC 툴에 반영해야 durable.
3. **4장 협박대상 = 조유리** (김채원 아님. 문장틀도 "입막음"→"약물로 붙들어"로 교정).
4. **공범 라벨 폐기** — 존재 자체가 "범인 외 1명"을 조사 0회에 누설. 진실세계
   accomplice 역할은 유지, 공란 라벨만 제거.
5. **상황판 = 상시 화이트보드** (필수). 브리프 §338 "5장 자동 전환"은 죽음.
6. **presence → 진술 도출 구현됨** (Option A): 무고=진실 도출, 범인만 claim(거짓말).
   opportunity 는 아직 손 fact 유지 — presence 도출(Option B)은 플레이테스트 후 보류.
7. **런타임 LLM 0 / 게임은 최종 채점 전까지 완전 침묵** — 모든 신규 화면에서 확인.

---

## Phase 진행

- [x] 1. 문서 모순 정리 (죽은 블록 표 + 브리프 대체 표시)
- [x] 2. 엔진↔프로토타입 데이터 동기화 (불일치 4건, 엔진 기준) + 검증기 강화(§6.5·§6.6)
- [ ] 3. 플레이테스트 빌드 수리 ← **DC 툴 동기화(3a)에서 막힘**
- [ ] 4. 플레이테스트 4명
- [ ] 5. 설계 보정
- [ ] 6. 프로덕션 구현

---

## 보류 중 (왜)

| 항목 | 왜 보류 |
|---|---|
| DC-SYNC-CHANGESET 7건 DC 툴 반영 | DC 저작 툴 필요 — Claude Code가 못 돌림. 사용자 작업 |
| DC 소스 → 리포 역반영 (floorplan 등) | export 가 컴파일 번들이라 메인 게임 소스가 없음. 방식 미정 |
| opportunity presence 도출 (Option B) | guilt·난이도에 직결. 플레이테스트로 계층 검증 후 |
| 죽은 풀 항목 (수면제·둔기·유산상속) | decoy 설계 결정 = 플레이테스트 후 |
| 실존 인물명 교체 | 상업 출시 전 |
