---
session_date: "2026-07-30"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN (OneDrive 아래) — 같은 날 네 번째 세션"
---

## Completed Work

**작업 트리 깨끗 · 게이트 7단 초록(exit 0) · port-check 308/320/12/어긋남 0(불변).**

### 0 — ⛳ 최우선 문서가 생겼다: `docs/MANIFESTO.md`

사용자가 확정한 **앱 개발 절대 원칙 16개 항.** 다른 문서와 충돌하면 **이긴다.**
`before-work` 읽기 **0번**으로 올렸다.

**§8 이 오래 열려 있던 질문을 닫았다** — 앱은 **AI API 를 쓰지 않는다.** 사용자의
챗봇(ChatGPT·Claude·Gemini·Kimi)에 복붙한다. **임시방편이 아니라 설계다.**
다음 세션은 「API 붙일까요」를 **묻지 않는다.**

§Ⅱ 에 **가름 축**을 세웠다:

> 「이 값이 틀렸다고 말하려면 **무엇과 대조**해야 하나?」
> 대답이 있으면 **사실**(코드) — **그리고 그 대답이 곧 가져야 할 꼴이다.**
> 없으면 **표현**(LLM). 코드는 자리만 만든다.

### 1 — 배제 사유가 거짓이었다 (생성 사건 **전건** · 말투 재러 갔다 걸렸다)

무고한 넷의 배제가 `'아침에 함께 도착했다'` 인데 생성기 구조는
「모였다→흩어졌다→다시 모였다」라 **아무도 아침에 도착하지 않는다.**
`revealedBy: []` 라 **조사 없이 공짜로** 나가고, 1장 보고서가 그 거짓을 쓰게 했다.

**뿌리는 산장의 문구를 전제 없이 가져온 것** — 산장은 넷이 실제로 오전에 도착하고
격자의 밤 슬롯이 부재다. 생성 세계는 부재가 없다.

여섯 자리(`e_mutual` record · `a_alibi` result · `f_no_*` · `f_no_*_ok` ·
1장 제목·opening · 1장 보고서). **회귀는 diff 로 증명** — 차분이 의도한 줄에만.
1장 문안(「가장 먼저 일어났다」)은 **저작이라 사용자에게 물었다.**

### 2 — 말투 왕복 재측정: **서식이 먹었다 (여섯 중 넷)**

```
                 조립 초안(12건)   공방(옛)   극장(새)   산장
최장/최단        1.16~1.56(1.42)    1.17      2.84 ✔    2.46
서로를 부름            0/5           0/5       3/5 ✔    4/5
호칭                    0             0         6  ✔     12
물음표                  0             0         0  ✘      8
끝맺음 종류            2종           2종       4종 ✔  4종(5중)
```

★ **초안 기준선을 먼저 세운 것이 값을 했다** — 「공방 1.17」을 바닥으로 읽고
있었는데 **초안이 이미 1.42**였다. 1.5 를 「올랐다」로 **오독할 뻔했다.**

### 3 — `npm run world-check` 신설

**소스를 안 뜯고 산출물을 센다**(AST 는 런타임 조립 문장을 놓친다). 네 세계로
뽑아 **세계를 타나 · 사건마다 변하나** 두 축. `UNIVERSAL` 은 `port-check` 의
`APP_ONLY` 와 같고 **양방향으로 문다.** ⛔ **게이트에 안 건다.**

찾은 것 셋(①의 작업 목록): `chapters[].report[]` · `chapters[].opening` · `terms[].note`.
**눌러보고 아니어서 선언한 것 다섯**(`창을 넘어 나갔다` 등).

### 4 — 구조 감사 1차 `docs/AUDIT-2026-07-30.md`

**결론: 개혁은 필요 없다.** 메울 곳 넷은 §Remaining 참조.

### 검증한 것

| | |
|---|---|
| 게이트 | 7단 exit 0 · 40/40 · port **308/320/12/어긋남 0** (불변) |
| 회귀 | 12건 기준선 ↔ 변경 후 diff — **의도한 줄에만** (좌표·이름·논리 0) |
| 극장 사건 | 검증 통과 · 5장 16공란 · hard |
| 말투 | `voice-check` 4파일 · 초안 12건 · 산장 재현 정확 |
| 용어 | 「판정」 전수 grep → 네 곳 고침 |

## In Progress

없음.

## Remaining Tasks

- [ ] **🎯 다음 하나 — 감사 A(엔진) + 들여오기를 같이** (사용자 지정)
      **`schema.ts`(538) 를 맨 앞에.** YAML↔타입 **경계**이고, 거기가 느슨하면
      07-30에 `types.ts` 읽고 내린 결론이 **전부 무의미해진다.**
      그다음 `generate.ts` 2306 · cli 계열 652. ≈ 3500줄
      **같은 세션에 들여오기를 붙인다** — `schema.ts`·`Generator.jsx` 를 건드리는
      일이라 감사하는 김에 지나간다. 감사만 하는 세 세션을 만들지 않는다
- [ ] **들여오기** — 적재 경로가 둘뿐이고 `<input type=file>`·`FileReader`·
      YAML 붙여넣기 **전수 0건**. 내보내기(`exportOne`)만 있어 **한 쌍의 반쪽**이 없다.
      **서버 불필요 · §8·§4 를 다 지킨다.** `exportOne` 의 왕복 대조(`out.same`)를
      들여올 때도 쓴다(§9)
- [ ] **감사 B** — `applyCase` 1166 · `Generator.jsx` 1323 ≈ 2500
- [ ] **감사 C** — `App.jsx` 나머지 3756 · scripts 462 ≈ 4200
- [ ] **① `Fact.basis` 도출** — `Fact.content: string` 이 오늘 버그의 타입 층 뿌리.
      같은 꼴 넷 더(`Evidence.description`·`record`·`Chapter.opening`·`result.body`)
- [ ] **`applyCase` 감시** — `world-check` 의 거울상. 사건 넷을 물려 **안 변하는 표**를 센다
- [ ] **⑤ 검열관** — §8 대로 복붙 브리프 · 「차단이 아니라 경고」.
      **재현 테스트가 이미 커밋돼 있다**(`closing-theater.yaml` 프롤로그 불일치)
- [ ] **§9 절반** — `Generator.jsx:970·1138` 이 오류 복사 버튼만 준다.
      매니페스토 §9 는 **완성된 수정 브리프 생성**을 요구한다
- [ ] 죽은 필드 — `bonusReveals`(읽는 곳 0) · `Chapter.reveals`(@deprecated인데
      `cli.ts:192` 가 읽는다) · `victim` 타입(셋 다 `people` 에 없다)
- [ ] `voice-check.mjs:36` `REF.endings: 5` — **잰 값은 4**. 인쇄되는 기준선이 거짓
- [ ] 물음표 0 — 절대 규칙 2·3 이 §읽어야 할 것 5 와 반대로 당기는 듯. **미검증**
- [ ] 린터 0개(§2·§3 미이행) · 서식의 `2.46` **열 곳** · `brief-check`(게이트 8단째)
- [ ] ⑤ 배포 — push 후 Vercel 이 실제로 도는지
- [ ] 관계도의 선 · 인터루드 도착물 4종 · 색·대비·질감

### ⛳ ~~사용자 결정 둘~~ — **눌러보니 둘 다 아니었다. 결정할 것이 없다**

- [x] ~~프로토타입 핀~~ — **안 묶고 있었다.** `port-check` 는 파일 경로 하나(`:38`)를
      정규식으로 훑어 **이름의 집합만** 비교하고, 주석(`:74~77`)이 **이미 메서드
      추출을 전제하고 조언까지 적어놨다** — *"인자 이름을 `V` 로 유지한다"*.
      쪼개려면 `APP` 을 파일 목록으로 바꾸는 **3줄**이면 된다.
      **남는 질문 「쪼갤까」는 → 지금은 아니다.** 단일 클래스가 결함의 원인이었던
      적이 없고(산장 누설은 전부 `applyCase` 의 **내용**), **감시 없이 리팩터하면
      리팩터가 흘린다**(*「변환기 자체가 틀린다」* 의 전례). **`applyCase` 감시가 먼저다**
- [x] ~~번역~~ — **상품(UGC)과 안 맞는다.** 외국 유저는 번역된 한국어 사건이 아니라
      **자기 언어로 짓기**를 원하고, 그건 UI 지역화 + 엔진 탈한국어화라
      `Text` 를 15필드에 더 까는 일이 아니다(조사 처리기가 뼈에 박혀 있다).
      **`Text` 는 지우지도 않는다** — 붙어 있는 8개가 전부 **LLM 이 채우는 자리**라
      층이 갈린 것이지 무작위가 아니다. **대신 아래 새 일감이 나왔다**

### 🎯 새 일감 — **EN 토글이 반쪽으로 살아 있다** (★ 눌러서 확인 ★)

```
EN 토글      화면 다섯 군데에 실제로 그려진다 (설정 패널·홈·세그먼트 셋)
산장         ✅ undefined 0 · 영문 1046자 · en 63개 — 잘 돈다
             ❌ 보고서 서술문만 한국어 (Chapter.report[].text 가 Text 아닌 string)
생성 사건    ❌ en 0개 — 생성기가 영문을 한 글자도 안 낸다
             (generate.ts 의 `en:` 9건은 전부 `len:` 오탐이다)
```

**UGC 가 상품인데 유저가 만드는 사건에는 영문이 0이다.** 토글이 약속하는 것을
**내장 데모 사건 하나만** 지킨다. 산장에서조차 한국어로 남는 게 **하필 보고서**다 —
`types.ts:663` 이 *"플레이어가 채우는 것은 칸이 아니라 문장"* 이라 적은 **게임의 본체**.

- [ ] **🎯 EN 토글을 감추거나 「데모 사건만」으로 표시한다** — 「없는 기능」보다
      **「깨진 기능」이 나쁘다.** 작고 급하다
- [ ] **「빌드타임 LLM 4곳」에서 번역을 뺀다** — 없는 것이 예산을 차지하고 있다
- [ ] 번역을 하기로 하면 `Chapter.report[].text` 가 **첫 자리**다
- ⚠ **생성 사건에서 EN 을 눌러보지는 않았다** — 폴백이 55군데 섞여 있어 어디까지
      한국어로 떨어지고 어디가 비는지는 눌러야 안다

## Blockers / Issues

- ⚠️ **감사의 88%가 남았다** (13,502 중 **11,808**). 안 읽은 곳에 대한 판단은
  `AUDIT-2026-07-30.md` 에 **넣지 않았다** — 첫머리에 깊이를 적어놨다.
  ⚠ 그 문서의 「`generate.ts` 15%」는 **틀렸다. 8%(203줄)다**
- ⚠️ **모순 검사가 0개다.** 산문 검사 넷(§9-7 a~d)이 전부 `text.includes(w)` —
  「확보 단어가 들어 있나」만 본다. **문장이 참인지는 한 번도 안 묻는다**
- ⚠️ **`applyCase` 1166줄·표 21개에 감시가 0이다.** 프로토타입에 없어서
  `port-check` 대상이 아니다. **산장 누설이 전부 여기서 살았다**
- ⚠️ **게이트가 JSX 를 안 본다** · **게이트가 서식을 한 글자도 안 읽는다**
- ⚠️ **PowerShell 이 파이프라인을 끊는다** — `npm run build | Select-Object -First N`
  이 **게이트 초록인데 exit -1** 로 보이게 한다. `Out-String` 으로 받는다
- ⚠️ **PowerShell 작업 디렉터리가 남는다** — `Set-Location <루트>` 를 앞에 붙인다
- ⚠️ **PowerShell 이 standalone `--` 를 삼킨다.** 엔진 CLI 는 **Bash** 로 돌린다
- ⚠️ **Bash 의 `/c/...` 경로가 node 에 그대로 가면 `C:\c\...` 가 된다.**
  node `-e` 에는 `C:/Users/...` 형태로 준다
- ⚠️ **`python3` 없음.** YAML 은 engine 에서 `node --input-type=module -e` + `js-yaml`
- ⚠️ **`read_page` 가 앱을 못 본다** · **스크린샷 막혀 있다** — `javascript_tool` 로 잰다
- ⚠️ **커밋 없이 `git stash` 금지** · **`localStorage.clear()` 금지**

## Notes for Next Session

- **`before-work` 순서 그대로.** `git pull` 이 맨 앞 · **직접 main 푸시**
- ★ **읽기 0번이 `docs/MANIFESTO.md` 다.** 충돌하면 그쪽이 이긴다.
  **API 를 붙일까 묻지 않는다** — §8 이 답했다
- ★ **「판정」을 함부로 쓰지 마라** — §절대 규칙의 「유죄 판정」과 충돌한다.
  개발 시점의 것은 **「가름」**이다 (`MANIFESTO.md` §가름 축의 표)
- **UGC 가 상품이다** (사용자 확정 2026-07-30). 그래서 오늘 잡은 배제 사유 거짓은
  개발자 불편이 아니라 **사용자가 만드는 모든 사건의 결함**이었다
- **극장 사건의 프롤로그 불일치는 일부러 남겼다** — ⑤검열관의 재현 테스트다
- **이 브라우저에 시험용 생성 사건이 남아 있다**(`gen-*`). 유용한 고정물이라 안 지웠다

## Files Modified

- `docs/MANIFESTO.md` — **신설.** §Ⅰ 사용자 원문 16항 · §Ⅱ 가름 축 · 어긋나는 세 꼴
  (A 사실을 문장으로 / B 사실을 LLM 이 / C 표현을 코드가) · 원칙별 대조 · 남은 일
- `docs/AUDIT-2026-07-30.md` — **신설.** 구조 감사 1차. **읽은 깊이를 첫머리에**
- `engine/src/generate.ts` — 배제 사유 여섯 자리 + 주석(자기가 경고한 형태에
  자기가 걸려 있었다는 기록)
- `engine/cases/closing-theater.yaml` — **신설.** 사건 3번 · 말투 측정 증거물
- `scripts/world-check.mjs` — **신설** · `package.json` 에 `world-check`
- `docs/MEMORY.md` · `docs/NEXT-ACTION.md` · `docs/SESSION-LOG.md` ·
  `.claude/commands/before-work.md`(읽기 0번 + 가름) · `.omc/worklog/latest.md`
