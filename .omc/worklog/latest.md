---
session_date: "2026-07-31"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 9단 초록(exit 0) · 생성 300/300 통과 · port-check 308/320/12/어긋남 0.**

> ★★ **사용자가 개념 하나를 바로잡았다 — 「생성기 = 유저용」** ★★
>
> *"클로드코드로 디테일한 캠페인을 만들 땐 생성기가 꼭 필요한 게 아니잖아.
> 내가 너한테 요청할 땐 생성기를 안 써도 된다는 거임."*
>
> 문서 전반이 「캠페인은 생성기로 만드는 것」처럼 읽히고 있었다. `MANIFESTO.md` 에
> **§사건이 생기는 길은 셋이다**(손저작 · 생성기 · 들여오기)를 세워 정본으로 삼았다.
> **구분은 이미 코드에 있었다** — 게이트가 `gen-check`(생성기)와 `tmpl-check`
> (손저작 서식)를 별개 단으로 갖고, 커밋된 4건 중 **둘이 생성기를 안 거쳤다.**
> 그리고 **새 결정이 아니라 `design-brief.md:442` 의 묻혔던 결정**이었다.

- **낡은 문서 셋을 닫았다** — `MANIFESTO §미이행`(§9 수정 브리프는 07-31 밤에 이미
  지어졌다: `fixBrief` · 호출부 넷) · `MEMORY.md` 의 **「낡은 다음 하나」를 경고하는
  문단이 스스로 그 부류였던 자리**(갱신이 아니라 삭제가 답) · `before-work` 7단→9단.
- **인용 출처 정정** — *"캠페인은 사람이 쓴다"* 를 `SYSTEM-DECISIONS.md` 소행이라
  적어놨는데 **그 파일에 그 문장이 없다.** 실물은 `design-brief.md:442` 하나다.
- ★★ **도면 다양성 셋을 한 세션에 닫았다** ★★
  ```
                전                      후 (300건 · npm run plan-check)
  현장 자리      4칸 · 두 칸에 90%   →   7칸 · 상위 두 칸 56%
  방 수         6칸 99.7%           →   5칸 99 · 6칸 117 · 7칸 84
  빈 자리        0 (불가능했다)       →   47% (노치 126 · 중정 15)
  최소 방 폭      130                 →   130      ← 안 좁아졌다
  통과율         100%                →   100%
  ```
  - **① 렌더러** `App.jsx` §sWalls 는 **덮인 횟수 ≥2** 인 구간만 내벽(4.5), §sPoche 는
    봉투 직사각형 대신 **방들의 합집합**(`martinez`)을 외곽선(6). 중정=구멍 링 · ㄱ자=꺾인 외곽선
  - **② 현장 자리** 문제가 「크기」가 아니라 **「자리」**였다 — 크기 1위 96%인데 자리는
    아홉 칸 중 넷. **사용자 결정으로 1~2위 중에서** 고른다. 밀실은 트릭 조건이라 안 흔든다
  - **③ 방 개수** 출처가 `chapters` 뿐인데 **표준 경로가 안 넘겨 언제나 5**였다.
    딸린 방을 **규모 ±1**. 장소를 안 늘려 조사 수·난이도·동선 풀이 안 움직인다
- **`npm run plan-check` 신설** — 도면 다양성 계측. 게이트에는 **안 건다**(기준치가
  없어 초록의 뜻이 없다 — `world-check`·`voice-check` 와 같은 자리).
- **검증기 §9-3i 문구 수정** — ⓒ가 *"선이 하나도 안 그려진다"* 고 경고하는데 그게
  그날 거짓이 됐다. 경고는 남기고 문구를 바꿨다(**안 고치면 검사가 거짓말을 한다**).

### 어떻게 확인했나

- **무회귀를 대조로 증명** — 옛/새 벽을 1단위 칸으로 펼쳐 집합 비교. 커밋된 5건 전부
  **사라진 칸 0 · 생긴 칸 0.** 「안 바뀐다」를 눈이 아니라 수로 말했다.
- **스크린샷이 막혀 있다**(브라우저 팬 미표시). 2026-07-27 선례대로 **DOM 의 path ·
  line · 라벨 박스를 직접 읽었다**: `gen-7` 왼벽 노치 · `gen-4` 구멍 링(중정) ·
  `gen-3` 현장 3위인데 붉은 기 정상 + poche 는 옛 형태(무회귀 분기) · `gen-4` 7칸에
  방 이름 전부 한 줄(13px).

## In Progress

없다. 셋 다 닫고 게이트 초록에서 멈췄다.

## Remaining Tasks

- [ ] 🎯 **테스터에게 URL 을 보낸다** — **사용자 손.** 두 세션째 열려 있다.
      도면 다양성이 닫혀 보낼 값어치는 더 커졌지만 **커밋된 4건은 그 전에 뽑힌 것**이라
      새 다양성을 보려면 사건을 다시 뽑아야 한다
- [ ] 🎯 **코드 쪽 다음 하나 — `scripts/world-check.mjs` 의 NUL 바이트**(한 줄).
      `'\x1f'` 로 바꾸면 끝
- [ ] 그 뒤는 `MANIFESTO.md` §남은 일 순서 — **① 도출로 바꾸기**(사실을 문장이 아니라
      `basis` 구조로) → **② ⑤검열관**. 서식의 `2.46` 열 곳도 거기 달렸다
- [ ] 사이드바 항목 접근성(`role`·`tabindex` 없는 `div`) — 별건으로 띄워뒀다

## Blockers / Issues

- ⚠️ **`scripts/world-check.mjs` 에 NUL 바이트 2개** → grep·ripgrep 이 이 파일만 조용히
  건너뛴다. 저장소 전수 검색이 거짓말을 한다. **미해결**(전 세션에서 이월).
- ⚠️ **`MIN_W` 가 자기가 안 지키는 값을 말한다** — 150인데 실제 바닥은 **130**
  (`bar` 기본 배분 `[190,150,130,130]` + `jitter` 바닥이 `Math.min`).
  *"가드가 조용히 포기"* 의 재발이고 **아직 살아 있다.** 결함으로 보진 않았다 —
  130에서 이름이 한 줄로 들어가는 것을 화면에서 확인했다. `plan-check` 가 매번 인쇄한다.
- ⚠️ **사이드바 항목이 키보드·스크린리더로 안 잡힌다** — a11y 트리에서
  「진술·현장·메모」가 통째로 빠져 자동 도구로도 못 눌렀다.
- ⚠️ `closing-theater` 는 **프롤로그 불일치를 일부러 남긴** 사건 — 테스터 목록에서 뺀다.
- ⚠️ 끝낸 사건도 홈에서 「진행 중」으로 보인다(`allSealed()` 를 다른 사건에 못 쓴다).
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.

## Notes for Next Session

- **계측을 짤 때 `generateCase` 만 부르면 전건이 예산 오류로 떨어진다.** 이번에
  「200/200 실패」를 보고서야 알았다 — **예산은 실험자(`orchestrate.run`)가 정한다.**
  게이트의 `--generate` 와 같은 길로 간다. `plan-check.ts` 머리에 적어뒀다.
- **다양성을 다시 손대면 `npm run plan-check` 부터 돌린다** — 위 표가 그대로 나온다.
- 처음 렌더러 판본이 **노치·중정 테두리를 두 번 그렸는데 색이 같아 화면에서 안 보였다.**
  별채처럼 `poche` 색이 다른 건물이었으면 드러났다. **「안 보인다」가 「없다」가 아니다.**
- `npm run verify -w engine -- --generate N --emit <dir>` 는 **npm 이 인자를 삼킨다.**
  `npx tsx engine/src/cli.ts --generate N --emit <dir>` 로 직접 부른다.

## Files Modified

```
docs/MANIFESTO.md               §사건이 생기는 길은 셋이다 신설 · §미이행 닫음 · §남은 일 재측정
docs/MEMORY.md                  이번 세션 절 · :103 낡은 스냅샷 삭제 · 오케스트레이터 범위 배너
                                · generate.ts 732→2,334줄 정정 · 인용 출처 정정
docs/NEXT-ACTION.md             머리말 · §다음 즉시 액션 · 다양성 ①②③ 닫음 · 도구 셋→넷
docs/SESSION-LOG.md             이번 세션 절(최신이 위)
.claude/commands/before-work.md 게이트 7단 → 9단
app/src/App.jsx                 §sWalls(덮인 횟수) · §sPoche(합집합 · martinez import)
engine/src/generate.ts          §빈 자리 신설 · 현장 1~2위 · 딸린 방 규모±1
engine/src/verifier.ts          §9-3i 뿌리 주석 · ⓒ 문구(없는 결함을 예고하지 않게)
engine/src/plan-check.ts        ★신규★ 도면 다양성 계측
package.json · engine/package.json   plan-check 스크립트
```
