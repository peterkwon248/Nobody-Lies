---
session_date: "2026-08-01 (밤)"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 12단 초록(exit 0) · 커밋 열여섯 · 푸시 완료 · 트리 깨끗.**

> ⛳ **날짜 주의** — 직전 워크로그의 「2026-08-02」는 **git 기준 08-01 오후**다.
> 라벨이 하루 앞서 있었고(07-28/07-29 때와 같은 부류 · 세 번째) 대응표를
> `NEXT-ACTION` 머리말과 `MEMORY` 머리에 달았다. **앞으로는 git 을 따른다.**

> ⚠ **`gen-baseline` 기준선을 세 번 다시 저장했다** — §9-14(빈 기록)과 아키타입 둘이
> **출력을 의도적으로 바꿨다.** 그냥 저장하지 않고 **옛 판을 옆에 떠서 64건 전수 대조**했다:
> ```
> §9-14       64/64 다른 필드: evidence 하나뿐 (그 안에서 record 만)
> 아키타입 6종  44건 동일 · 바뀐 20건 전부 trick 이 함께 바뀌었다
> 아키타입 7종  같은 모양
> ```
> **`trick` 이 안 바뀌었는데 달라진 건은 0** 이다 — 딴 게 안 흘렀다는 뜻이다.

### ★★ 오늘의 줄기 — 「초록인데 아무것도 안 물고 있다」가 셋 ★★

```
옛 §5 (지목 장 조합 수 ≥ 30)   한 번도 발화한 적 없다. 게다가 엉뚱한 장을 봤다
§6.8 (페어플레이 · 인물 공란)   손저작 16개 중 13개를 아무 답이나 통과시켰다
clue-check ①·②                 배선을 지워도 초록 · 종료 조건 뒷항이 안 심겨 있었다
```

셋 다 **「걸린 적이 없다 = 잘 돌아간다」**로 읽히고 있었다. 규칙을 `MEMORY.md`
**§검사를 믿는 법**으로 성문화했고, **재는 도구까지 세웠다**(아래 ⑥).

### ① 공란 도출률 35/61(57%) → 55/61(90%) — `26dc513`

세 갈래가 **서로 다른 병이었다.** 「전부 같은 결함」이 아니었다:

| | 무엇 | 어떻게 |
|---|---|---|
| ① | 서술문이 근거 없이 단언 — *"가장 먼저 도착했다"* | **식별 고리를 걸었다.** 고리는 이미 있었고 서술문이 안 쓰고 있었다 |
| ② | 기록이 답을 안 말한다 | 기존 문장에 한 마디씩 이어 붙였다 |
| ③ | **규칙이 없어서 못 낸 것** | **`R8 씨앗 단어` 신설** — 산장의 「테이프」는 결함이 아니라 전제였다 |

사건별: `closing-theater` 7→0 · `pipe-organ` 6→0 · `practice-room` 5→0 ·
`mountain-lodge` 8→6(**일부러 남김** — 아래 Remaining).

⚠ **`R8` 은 반드시 `R4` 뒤에 둔다** — 씨앗이면서 조사로도 얻는 단어가 있다(생성 `tool`).
앞에 두면 유료 경로가 있는데도 비용 0 으로 증명돼 **조사가 필요 없어 보인다.**

### ② `verifier §5` 를 갈아 끼웠다 — `f04046b`

```
전   §5    지목 장의 blanks 조합 수 ≥ 30      오류
후   §5-b  모든 장 · 공란마다 사슬이 서는가     경고 (weakBlanks)
```

★ **명제만 틀린 게 아니라 보는 장이 엉뚱했다** — 지목 장은 커밋 4건 전부 **도출 100%**고
찍기 26개는 전부 1~4장에 있었다. 이제 검증기와 `clue-check` 이 **같은 함수**를 부른다.

### ③ `clue-check` 이 두 단 → **세 단** — `f04046b` · `b0ff256`

- **③ 배선** — ①②가 `weakBlanks` 를 **직접** 부르므로 **검증기 §5-b 를 지워도 초록**이었다.
  문안은 `verifier` 가 `weaknessWarning()` 으로 export 하고 `clue-check` 이 대조한다.
- **② 에 `L1` 누설 심기** — M1·M2 는 둘 다 `guess` 갈래라 종료 조건의 **뒷항이 한 번도
  안 심겨 있었다.** `isDeclaredPremise` 가 항상 참이어도 초록이었다.

### ④ §9-14(빈 기록)가 **전건에서** 닫혔다 — `6498be4` · `c99c26f`

생성 **40/40** · **저작 서식** · 산장 3 · 연습실 2. 산문 왕복을 거친 둘만 원래 0 이었다.

### ⑤ 트릭 아키타입 **5종 → 7종** — `52ec664` · `fd5c2b1`

- **`staged_intrusion`(외부인 위장)** — 나머지가 사인·시각·장소를 속이는데 이것은
  **용의자 집합**을 속인다. `identity_swap` 과 달리 **목록이 참인 채로** 바깥을 가리키므로
  공란 체계를 안 건드린다.
- **`staged_accident`(사고사 위장)** — `staged_suicide` 와 인상 종류는 같고(`death`)
  주장·부품이 다르다(이탈 방법 없음).
- 70건 분포 **6~12건으로 고르다** — 새 둘이 기존 다섯을 밀어내지 않았다.

### ⑥ ★ 침묵 검사를 세웠다 — `npm run mute-check` (`98e81e1` · `61c73ee`)

```
verifier.ts 의 주장 117개
울렸다      64      55%
안 울렸다   53      오류 39 · 경고 14
```

①소스에서 `errors/warnings.push` 자리를 찾고 ②성한 사건 ③**61종으로 망가뜨린 사건**에
돌려 ④안 울린 자리를 인쇄한다. ★ **뮤테이션을 두 번에 나눠 넣은 것 자체가 결과다** —
첫 판 36종에 **33%**, 침묵 목록을 겨냥해 25종을 더하니 **55%**.

### ⑦ 손으로 베낀 목록 둘을 도출로 — `52ec664` · `61c73ee`

`schema.ts` 의 `TRICK_TYPES`(→ `Object.keys(ARCHETYPES)`)와 `IllusionKind` 다섯
(→ `ILLUSION_KINDS` 를 값으로 두고 타입을 뽑는다). **오류 문안도 배열에서 낸다.**

### ⑧ 낡은 문서 다섯 — `57f39f3` · `33dce04` · `03e361e` · `a9536cf`

`MANIFESTO` 가 **자기 자신과 모순**이었고(§5 「전수로 안 세봤다」 ↔ 60줄 위 「세어서
결론냈다」), `martinez` 를 「안 썼다」고 적어뒀는데 `App.jsx:3159` 가 쓰고,
`STATUS` 는 게이트 11단이고, **저작 서식**이 찍기 하나와 낡은 명제 하나를 가르쳤고,
`NEXT-ACTION` 이 「트릭 지금 5종」에서 멈춰 있었다.

### ⛔ 재보니 전제가 무너진 것 넷 — **전부 짓기 전에 재서 안 지었다**

```
「같은 문장의 다른 공란이 인물을 집어준다」   0/16 — 다리가 없다
「빈 기록과 R6 미증명이 겹친다」              안 겹친다 — 서로 다른 물증이다
「§5-b 26개는 소음이다」                      근거가 §6.8 의 통과였는데 그게 무의미했다
「trick.props 검사가 죽었다」                 schema.ts:528 이 보고 있었다 — 도구 사거리 밖
```

## In Progress

없다. 게이트 초록 · 트리 깨끗 · 푸시 완료에서 멈췄다.

## Remaining Tasks

- [ ] 🎯 **다음 하나 — 침묵 53개를 무리째 깨운다** (`npm run mute-check`)
      **지금 값: 64/117 (55%) · 침묵 53(오류 39 · 경고 14).** 도구가 매번 같은 표를
      내므로 **진도는 % 로 스스로 드러난다** — 장부를 따로 두지 않는다.
      **하나를 세 갈래로 가른다**: ⓐ 스키마가 본다(사거리 밖 · 그대로 둔다) ·
      ⓑ 뮤테이션이 못 닿았다(`MUTATORS` 에 더한다) · ⓒ 진짜 안 문다(검사를 고친다).
      ⚠ **ⓐ를 ⓒ로 착각하지 마라** — 오늘 한 번 헛짚었다. **안 울린 자리는
      `schema.ts` 를 먼저 grep 한다.**
      ★ **첫 배치는 평면도 무리 11개** — `verifier.ts` 834·853·856·863·888·893·912·
      995·1005·1010·1049. 지금 뮤테이션이 「평면도를 지운다」뿐이라 **너무 거칠다**.
      필요한 것은 **국소 파손**(방을 건물 밖으로 · 방 둘 겹치기 · 문 빼기 ·
      `scene` 표식 떼기 · `walks` 의 from·to 지우기 · 고정물을 없는 장소로).
      **그다음 배치 둘** — `reveals` 없는 id(653·655·659·691) · 서술문 참조(519·521).
      ⛔ **100% 가 목표가 아니다** — ⓐ가 몇 개인지 아직 안 셌다. **먼저 그것부터 세면**
      도달 가능한 천장이 나온다(침묵 53을 `schema.ts` 와 대조. 싸다).
- [ ] 🔴 **테스터가 뭐라고 했는지를 받아 적는다 — `docs/PLAYTEST.md` 신설**
      사용자 정정: *"나는 테스트를 안 하고 있고 **지인들에게 맡기고 있다(범인 모름)**"*.
      **그들이 뭐라 했는지가 저장소 어디에도 없다.** 코드가 못 재는 축
      (재미 · 난이도 체감 · 누설 감각)은 **전부 여기서만 온다.**
      ⛳ **사용자 결정으로 지금은 접어뒀다** — 코드로 되는 것부터 한다.
- [ ] **`mountain-lodge` 의 남은 6개** — 1장 인물 둘 · 2장 마지막목격자 · 2장 장소 ·
      2장 인물 · 4장 인물. **일부러 안 고쳤다** — 손으로 쓴 산문이고 근거가 진술 안에
      실제로 있다. **규칙이 못 읽을 뿐**이라 산문을 규칙에 맞추면 개악이다.
- [ ] **여덟째 아키타입** — 「흉기 바꿔치기」는 현장의 `e_tool` 이 가짜여야 해서
      **`baseEvidence`(기반 층)를 건드린다.** 여섯·일곱째가 쌌던 이유는 **자국을 더할
      뿐**이라서다. ⚠ **7종이 충분한지는 테스터가 답한다.**
- [ ] 🎯 **행렬 빈칸 19 + 부분 8** (`npm run matrix`) — 가장 큰 셋이 전부 산문 층.
      ⚠ 어휘 봉쇄를 순진하게 만들면 안 된다(20/20 거짓 양성).
- [ ] **§번호 → 행렬 좌표 개명** — **오늘 더 나빠졌다.** `verifier.ts` 에 `// 5.` 가 둘인데
      `§5-b` 를 더했고, `§6.8` 은 원래부터 둘이다(페어플레이 · 인터루드).
- [ ] `es-hangul` 이관 — 조사 규칙이 **다섯 벌**. 오늘 여섯 벌째를 안 만들려고 문안을 우회했다
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌 · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **테스터 피드백이 저장소에 0건이다** — 사용자 결정으로 지금은 접어뒀다.
- ⚠️ **손저작 6개는 코드가 판정할 수단이 없다** — 결함이 아니라 **담당이 다른 것**.
- ⚠️ **`mute-check` 는 `schema.ts` 를 못 본다** — 침묵 53에 스키마 몫이 섞여 있다.
  **몇 개인지 아직 안 셌다.**
- ⚠️ **`R8` 을 `R4` 앞으로 옮기면 조용히 망가진다** — 주석에 박아뒀다.
- ⚠️ **날짜 라벨이 git 보다 +1일** — 과거 라벨은 안 고치고 대응표만 뒀다(사용자 결정).
- ⚠️ **DS 번들이 `#root` 에 자기 앱을 마운트한다** · **사이드바 밖 클릭 137곳** — 별건.
- ⚠️ `closing-theater` 는 **프롤로그 불일치를 일부러 남긴** 사건 — 테스터 목록에서 뺀다.
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.

## Notes for Next Session

- ★★ **「초록」을 믿지 마라 — 심어봤는지를 믿어라** ★★ 오늘 셋이 나왔고 셋 다
  **한 번도 안 울려서** 잘 돌아가는 것처럼 보였다. `MEMORY §검사를 믿는 법`.
- ★ **무의미한 검사를 찾는 법: 답을 바꿔치기해서 다시 물어본다.** §6.8 이 그렇게 잡혔다.
  **세는 것으로는 못 잡는다** — 좋은 검사도 평소엔 안 운다.
- ★ **도구의 사거리를 도구 안에 적는다** — `mute-check` 가 스키마를 못 본다는 것을
  안 적었으면 오늘의 헛짚음이 다음 세션에 그대로 재현된다.
- ★ **내가 저작할 때 LLM 을 안 쓰는 게 아니다** — 「런타임 LLM 0」은 **앱이 API 를
  부르지 말라**는 것이고 §14 는 **LLM 이 규칙을 정하지 말라**는 것이다. **산문을 쓰는 것은
  내 일이다.** 「산문 왕복이 필요하다」는 병목은 **내가 저자일 때는 없다.**
- ★ **사용자가 축을 셋으로 갈랐다** — ①재미=테스터 · ②산문=LLM · **③트릭·범인·논리=코드**.
  ③ 중 **범인 유일성과 공란 도출은 이미 100%**, 트릭 수는 7종. **코드 소관은 거의 찼다.**
- ⛔ **`generateCase` 에서 throw 하지 마라** — 브라우저가 부르므로 유저 화면이 통째로 빈다.

## 참고 — 외부 공유용 정리

`docs/STATUS-2026-08-01.md` — 게이트 절은 12단으로 고쳤다.
⚠ **§2-2 의 손저작 57% 는 이제 90%, 트릭 5종은 7종이다** — 다음에 갱신한다.

## Files Modified

```
engine/src/mute-check.ts        ★신규★ 침묵 검사 (뮤테이션 61종 · 사거리 주석)
engine/src/verifier.ts          §5 교체(§5-b) · weaknessWarning export · §6.8 실행부 제거
engine/src/proof.ts             ★R8 씨앗 단어★ (R4 뒤에 둔다)
engine/src/clues.ts             isDeclaredPremise export · PREMISE 에 R8
engine/src/clue-check.ts        ③ 배선 검사 신설 · ② 에 L1 누설 심기
engine/src/proof-check.ts       비용 0 을 전제/누설로 갈라 인쇄
engine/src/generate.ts          §9-14 기록 셋 · 아키타입 둘(staged_intrusion·staged_accident)
engine/src/types.ts             TrickType 둘 · ARCHETYPES 계약 둘 · ILLUSION_KINDS
engine/src/schema.ts            TRICK_TYPES·IllusionKind 를 도출로
engine/templates/case-template.yaml   scene_state · 기록 셋 · 낡은 명제 주석
engine/cases/*.yaml             넷 다 — scene_state · 소지품 제목 · 서술문 · 기록
package.json · engine/package.json    mute-check 스크립트
docs/MANIFESTO.md · MEMORY.md · NEXT-ACTION.md · SESSION-LOG.md · STATUS-2026-08-01.md
.claude/commands/before-work.md clue-check 두 단 → 세 단
.omc/worklog/latest.md          이 파일
```
