---
session_date: "2026-08-06"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 15단 exit 0 · 린트 경고 0 · 커밋 열다섯 · 트리 깨끗.**

⚠ **머신이 바뀌었다** — 직전 두 세션은 `DESKTOP-JCJTAH8`(Desktop), 이번은
`KWONKYUNGHUN`(OneDrive). `npm install` 이 필요했고 `"peer": true` 차분이
예고대로 나서 그 자리에서 되돌렸다.

### ★★ 아침에 잡은 것 — 워크로그가 두 세션 낡아 있었다 ★★

`before-work` 가 잡았다. `latest.md` 의 마지막 커밋이 `b1bd6d0` 인데 그 뒤로
after-work 커밋이 **둘 더 지나갔다**(`94f25c1` · `a59e413`). 내용이
「⑤검열관 첫 실행」에 멈춰 상황판 삭제·감사 20건·수리-A 가 통째로 없었다.

→ **`.claude/commands/after-work.md` 에 검사 둘을 걸었다**(`d862592`):
- **§2-b** 커밋 전 — `git status --short` 로 인계 파일 넷을 **센다**
- **§4** 푸시 후 — 파일별 마지막 커밋 **해시가 전부 같은지** 본다

⛳ **날짜로 세려다 실측에 반증됐다** — 실제 누락 사례에 걸어보니 넷이 전부
「2026-08-05」였다(하루에 세션이 셋 돈 날이라 **낡은 것도 오늘**이었다).
**가르는 것은 날짜가 아니라 해시다.**

### 감사 결산 — 깨짐 0 · 밋밋함 0

```
                감사(08-05)   지금
깨짐                 3          0    #1 #2 #3  (+ 수리 중 발견한 #23 #24 도 닫음)
밋밋함               3          0    #18 #19 #20
터치 타깃 <44       72          0    4폭 전부
간격 그리드 밖     447          2    남은 둘은 좌표 (간격 어휘 0)
하드코딩 hex       100         23    74는 오계수 · 15는 범위 밖 확정
strokeWidth       12종       11종    아이콘은 1종(1.6 · DS 표준)
focus-visible        0          1
animation            0          2    + prefers-reduced-motion 1
캡처                52         60    못 잰 것 4 → 0
```

### ★★ 오늘의 줄기 — 세는 것이 틀리면 계획이 거짓말 위에 선다 ★★

**오계수 넷. 전부 무언가를 떠받치고 있었다.**

```
hex 100 중 74    var() 폴백을 하드코딩으로 셌다 → 스킨 범위 판단 전체가 그 위에
간격 244 → 447   단축 표기 뒷값(`padding:'2px 8px'` 의 8)을 못 봤다
간격 케밥 85     계수기·수리가 **같은 정규식**이라 `margin-top` 을 둘 다 못 봤다
좌표 87 → 2      `margin-left:` 의 꼬리가 `left:` 로 잡혔다
```

★★ **새 규칙** — `MEMORY.md` §검산이 피검산과 같은 길을 걸으면 안 된다 에 한 단계
추가: **경로를 갈라도 표현이 같으면 사각도 같다.** 증상이 특히 나쁘다 —
검사가 「0」을 인쇄하므로 **수리가 끝난 것으로 보인다.**

### ★★ 둘째 줄기 — 보이게 하는 수리가 곧 측정기다 ★★

#3(초점 링)을 그리자 깨짐 둘이 드러났다.
- **#23** 하단 탭 다섯이 `tabIndex:-1` — 좁은 폭에서 **화면 이동이 키보드로 불가능**
- **#24** 상단 버튼이 바 밖으로 4px — **#2 에서 내가 만든 결함**이고 그때 보고도
  틀렸다(「상단 바 36 → 44」의 그 값은 **버튼의 래퍼**였다)

### 중간 공개 3단계 (`DESIGN-NOTES` §확정 결정 2·3 구현)

```
대  새 장 진입(기존) · 트릭 첫 균열(신설·규칙 판정) · 지목 직전(모달 → 전체 화면)
중  같은 인상의 후속 균열 — 반화면 카드 · 시안 A(rise) · **자동 닫힘 타이머 없음**
소  일반 조사 결과 — 토스트 현행
```

⛔ **배선을 두 번 잘못 지었고 캡처를 열어봐서 잡았다** — ⓐ `state.evidence` 는 앱
로컬 이름(`annexPhone`)이고 `brokenBy` 는 엔진 id(`e_note`)라 **영영 참이 안 됐다**
(→ `CASE_ACTIONS.gives` 로 이전, `invLog` 에서 도출). ⓑ 카드 JSX 가 `isIntro` 안이라
**자유 플레이 중엔 안 떴다**(→ 최상위). **둘 다 게이트도 검사도 초록이었다.**

## In Progress

**없다. 모든 변경이 커밋됐고 게이트 15단이 초록이다.**

## Remaining Tasks

- [ ] 🎯 **수리-B 우선순위 찍기** — 감사 표가 깨끗하다. 어긋남 묶음 다섯 중
      경훈이 순서를 찍는다 (`NEXT-ACTION.md` §다음 즉시 액션에 표로 있다)
- [ ] **🅿 DS 결정 둘** — ⓐ 비반전 「색 위의 잉크」 토큰(기존 토큰은 라이트에서
      뒤집혀 쓰면 라이트가 깨진다) ⓑ 알파 틴트 기제(`color-mix` vs `--x-rgb`).
      **이 둘이 raw 값 ~37개를 푼다** — 토큰화 잔여의 입구다
- [ ] **🅿 트릭 붕괴 문안 저작** — 대·중이 `impression` 기반 **잠정**이고 화면에
      「※ 이 문안은 잠정입니다」로 표기돼 있다. **테스터에게 잠정 문안대로 나가는
      것은 허용됐다**(08-06 확정)
- [ ] **🅿 `--accent` 위 글자 넷** — `#0A0A0B` ↔ `#fff` 로 **같은 역할이 정반대 두 값**.
      DS 계약(`--fg-on-accent`)을 따르면 다크에서 모양이 바뀐다(저작 판단)
- [ ] **A/B 돌리기** — 배선 끝났다. `?reveal=heavy` 로 테스터 절반.
      PLAYTEST 질문 등재됨 — **「어디서」를 같이 받아야 한다**(끊긴 자리가 일반
      조사 결과면 「소」가 무겁고 장 전환이면 「대」가 과하다 — **처방이 반대**다)
- [ ] **#25 탭 순서** — 데스크톱에서 `포기`(사이드바 끝) 다음이 `뒤로`(상단)다.
      마크업 순서 문제라 **구조 백로그**(고치지 않기로 확정)
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌(콘솔에 상주) · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **막힌 것 없음.**
- ⚠️ **#12(인라인 713)는 🅇 기각이다** — 되살리지 마라. 스킨 관련 402개 중
  **398개가 이미 `var(--토큰)`** 을 지나므로 인라인은 스킨을 막지 않는다.
  고유 432종 중 **330종이 1회용**이라 클래스로 옮기면 1회용 클래스 330개가 된다.
  **방아쇠**: 다음 스케일 변경이 오면 그때 토큰부터 세운다.
- ⚠️ **`breakOn` 필드도 🅇 기각** — 트릭 붕괴를 첫/후속으로 가르니 any/all 물음이
  사라졌다. 데이터에 필드를 더하지 않는다.
- ⚠️ **인물 색 15곳은 스킨 불변 · 범위 밖 확정** — 식별색은 정보다.
- ⚠️ **시안 B(`?reveal=pop`)는 지웠다** — A(rise)로 확정. 되살리지 마라.
- ⚠️ **날짜 라벨** — 이번 세션은 git 과 라벨이 같다(**2026-08-06**).

## Notes for Next Session

- ⛔ **측정기가 다섯이다** — `design-audit` · `token-audit` · `focus-check` ·
  `reveal-check` · `reveal-shots`. **전부 `npm run dev` 가 떠 있어야 한다.**
  게이트 15단에는 안 들어간다(dev 서버가 필요하므로).
- ⛔ **`focus-check` 은 진짜 키보드 순회다** — JS `.focus()` 로는 `:focus-visible`
  이 안 뜬다. 브라우저 팬이 안 보이면 CDP 키 입력이 안 가므로 이 스크립트를 쓴다.
- ⛔ **CDP 뷰포트 변경은 페이지에 `resize` 를 안 쏜다** — 리사이즈 후 레이아웃이
  안 따라오면 **앱 버그가 아니라 하니스 특성**이다. 새로고침하거나
  `window.dispatchEvent(new Event('resize'))` 를 쏘고 재라. 오늘 이걸로 한 번 오진했다.
- ★ **연출·화면 작업은 캡처를 열어봐야 끝난다** — 오늘 배선 둘이 게이트도 검사도
  초록인 채로 안 뜨고 있었다(큐는 채워지고 있었으니까). §이식 규칙 4 가 연출에도 걸린다.
- ★ **`port-check` 이 기능 손실을 잡는다** — 지목 직전을 승격하면서 미채움 공란
  경고(`finishUnfilled`)를 떨어뜨렸는데 그것이 물었다. 새 갈래를 만들면
  `APP_ONLY` 에 **그 커밋에서** 선언한다(오늘 `card.open`·`interlude.isProvisional` 둘).
- ★ **죽은 코드를 `{(false)?…}` 로 숨기지 않는다** — 통째로 지운다. 오늘 그러다
  고아 JSX 가 남았고 게이트가 잡았다.

## Files Modified

**이 커밋에 포함됨** (인계 파일 넷):
`docs/MEMORY.md` · `docs/NEXT-ACTION.md` · `docs/SESSION-LOG.md` · `.omc/worklog/latest.md`

**앞선 커밋 열다섯에 이미 포함됨**:
`app/src/App.jsx` · `app/src/styles.css` · `app/src/main.jsx` · `package.json` ·
`.claude/commands/after-work.md` · `scripts/{design-audit,token-audit,port-check,focus-check,reveal-check,reveal-shots}.mjs` ·
`docs/{DESIGN-AUDIT-2026-08,DESIGN-NOTES,PLAYTEST,audit-data,audit-tokens}.*`

⛳ `docs/audit-shots/*.png` 는 `.gitignore` 다 — 측정기를 다시 돌리면 생긴다.
