---
session_date: "2026-08-05 (git 확인 · 같은 날 두 번째 세션)"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kwonkyunghun/Desktop/Nobody Lies"
machine: "DESKTOP-JCJTAH8"
---

## Completed Work

**게이트 15단 초록(exit 0) · 커밋 다섯 · 트리 깨끗 · 4단계 계획 1번 완결.**

⚠ **머신이 바뀌었다** — 직전 세션은 `KWONKYUNGHUN`(OneDrive), 이번은
`DESKTOP-JCJTAH8`(Desktop). 게이트 15단이 이 머신에서 **0회 실행**이었고,
사용자 지시로 **기준선부터 잡고 시작했다**(exit 0 확인). 그 덕에 이후의 빨강이
전부 코드 몫으로 귀속됐다.
⛳ 내가 "워크스페이스 호이스팅이니 `npm install` 불필요"라고 **디렉터리 존재만 보고
말했는데**, 그건 추정이었다. 맞았다는 것은 게이트가 통과한 뒤에 알았다.

### ★★ 오늘의 줄기 — 「참조가 있는데 참조 대상이 비면 조용하다」 ★★

**하루에 다섯 번 나왔다. 전부 같은 형태다.**

```
poolFor          모르는 라벨 → 빈 배열 → proveBlanks 가 공란을 통째로 건너뜀   (아침 인계)
discoveryTime    asks 없음 → 솔버 undecidable → §6 교차표에서 누락
협박대상          위와 같음 — 「경고 없음」과 「안 봤음」이 화면에서 같았다
pointsAt 빈 값    asks: recordPlace 는 있는데 evidence.pointsAt 이 없음 → 누락
yieldsTerms      확보 단어 넷 — length !== 1 이면 같은 경로로 조용히 사라진다  (미리 죽였다)
```

⛔ **내가 「62/62 라 숨는 자리가 사라졌다」고 보고했고 같은 날 반증됐다.**
정확한 정의는 **`asks` 없음 · 또는 `asks` 가 가리키는 값이 빔**이다.
**참조를 채우는 것과 참조 대상을 채우는 것은 다른 일이다.**
→ `docs/MEMORY.md` §참조가 있는데 참조 대상이 비면 조용하다 (계보 + 일반 규칙 표).
**다섯 종이 스키마에서 죽었으므로 이 병의 계보는 여기서 끝난 것으로 본다**(사용자 확정).

### ★★ gen-baseline 이 **사용자의 지시를 반증했다** ★★

인계 조건 1이 *"생성기 수정이 본체다 — `:906` 템플릿이 장소를 말하게 되는 게
재발 방지"* 였다. 그대로 고쳤는데 **`gen-baseline` 이 회귀 0 을 냈다** —
`:906` 은 `REC_STRAND` 루프(`:2146`)가 덮어쓰는 **자리표**라 출력이 한 글자도
안 바뀐다. 실제 템플릿은 **처음부터 옳았다**(홀수 가닥 = `${places.hall}이 적힌
줄이 남아 있었다`).

★ **닻을 떨어뜨린 것은 생성기가 아니라 산문 왕복이다** — `pipe-organ` 의 저작
산문은 「공방 식당」을 지켰고 `closing-theater` 는 잃었다.

> ★★ **죽은 코드를 재발 방지라고 커밋할 뻔했다.** ★★ 조건 3(`gen-baseline` 재실행
> 한 묶음)이 조건 1의 오진을 잡았다. **지시를 준 사람까지 삼각 검증 안에 있다**는
> 것이 또 실측됐다 — 같은 날 사용자의 ①(「4시 50분 입장 = 발견자」)도 반증됐다.

자리표 둘(`:906` record · 결과문)에 **「여기 고쳐도 출력이 안 바뀐다」**를 주석으로
박았다. 다음 사람이 같은 데 안 빠지게.

### 커밋 다섯

```
d82e0a4  factValue 다섯 — 값을 적었더니 아무도 안 보고 있었다
5a56cca  practice-room 1장 시각 결함 수리 — asks 를 달자 숨어 있던 공란이 드러났다
b9e6cdb  personAt — 격자 읽기 어휘 하나로 cvs 를 닫았다 (①에서 끝났다)
126638c  폴백 인쇄 문구를 실측에 맞춘다 — 「asks 를 채우면 0」이 틀렸다
16ab24a  pointsAt — 「hall 둘」을 닫고, 이 부류의 다섯째까지 스키마에서 죽였다
```

### 숫자 (전 → 후)

```
asks              55/62 → 62/62   전건이 물음을 선언한다
Fact.value            0 → 5 (산장) · 생성기는 건당 2
라벨 폴백              3 → 1       남은 1 = 산장 2장 '시각'(구조상 남는다 · 결함 아님)
무검사 공란            0 → 0       중간에 두 번 드러났고 같은 커밋에서 닫았다
누설                  5 → 5
gen-baseline                   회귀 0 (64건)
게이트 15단        exit 0 → exit 0
```

### 규칙 승격 — 새 필드는 **다섯 곳이 한 벌**

`types · schema · to-yaml · generate` + **⑤ 방어 검사(같은 커밋)**.
`pointsAt`(사용자 경고) · `factValue`(같은 세션에 재현) · `factSubject`(소급 15개)
**세 번이면 패턴이다.** → `docs/MEMORY.md` §새 필드는 다섯 곳이 한 벌.

★ **뮤테이션이 필요를 증명했다** — `f_ring_goods.value` 를 답과 어긋나게 고쳐도
게이트 15단이 **전부 초록**이었다. 검사를 넣고 나서 같은 뮤테이션이 exit 1 로 물렸다.

★ **뮤테이션이 나를 두 번 잡았다** — ⓐ `npm run verify` 로 심었는데 초록이라
검사를 의심했는데 **그 명령은 산장만 돈다**(뮤테이션이 파서를 안 거쳤다).
**심는 것도 경로를 확인해야 한다.** ⓑ 내 폴백 예상(3→0)이 틀렸다.

### ⑤검열관 첫 실행 — **①을 잡았다**

**예상을 먼저 적고 돌렸다**(사용자 지시 · `scratchpad/censor-prediction.md`).
근거: 검열관은 비결정적이라 예상 없이 돌리면 무엇이 나와도 「그런가 보다」가 된다.

```
① 잡아야 할 것   prologue[1] 「다음 날 아침, 분장실에서 임소현이 발견됐다」
                 ↔ 시간축이 t2「자정을 조금 넘긴 무렵」에서 끝난다
                 → ✅ 잡았다. 통과 1 · 기각 0 · exit 1(경고가 살아 있다)
② 잡으면 안 될 것  오늘 고친 e_rec2.record
                 → ⚠ **구조적으로 범위 밖이라 vacuous 다** (아래)
③ 모르는 것      범위 자체가 수확이었다
```

★★ **검열 대상 범위 실측 — `chapter` 10 · `prologue` 4 · `reveal` 4** ★★
**`evidence.record` 와 `epilogue` 는 범위 밖이다.** ②가 「판정을 잘해서」 안 걸린 게
아니라 **아예 안 본다.** 그리고 **오늘 닻이 떨어진 자리가 정확히 그 범위 밖**이었다
(`e_rec2.record`). 검열관이 본업을 하긴 하는데 **보는 데만 한다.**

## In Progress

**없다. 모든 변경이 커밋됐고 게이트 15단이 초록이다.**

## Remaining Tasks

- [ ] 🎯 **다음은 코드가 아니다 (사용자 확정)** — 인프라 주간은 검열관 첫 실행으로
      마침표다. 다음 우선순위는 **기계가 못 재는 축**이다:
      ① **테스터 데이터** — 지인들이 이미 플레이 중인데 저장소에 0건. `docs/PLAYTEST.md`
      ② **용의자 프로필 채널 43/44 사건에서 빔** — `action.clues` 가 산장에만 12개
- [ ] **3번(명제 추출 확장)** — 착수 근거는 **성립했다**(①을 잡았으므로). 단
      **「할 수 있다」지 「지금 한다」가 아니다**(사용자). 테스터가 산문 문제를
      실측해준 뒤에 지어도 늦지 않다.
- [ ] **검열관 범위** — `evidence.record`·`epilogue` 를 넣을지. 오늘 실측이 근거다.
      다만 스키마의 닻 대조가 `recordPlace` 는 이미 파스 시점에 문다(중복 여부 판단 필요)
- [ ] 열린 자리 셋 (매니페스토 §알려진 열린 자리 · **갈라질 조건과 수리 처방까지 적혀 있다**)
      산장 2장 '시각'이 R6 산문 채널 · `discoveryTime` 의미론 · `factValue` 의 domain
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌 · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **막힌 것 없음.**
- ⚠️ **2장 `free_chapter` 는 선언하지 않기로 확정** — 1장의 무료는 YAML 주석이 받치는
  문서화된 설계지만 **2장의 무료는 `revealed_by: []` 가 만든 우연한 상태**다.
  **확인 안 된 의도를 선언하면 그것이 거짓 문서다**(사용자).
- ⚠️ **`factValue` 의 `domain` 필드는 짓지 않기로 확정** — 실측된 결함 0.
  실패 사례 없이 짓지 않는다. 갈라질 조건은 매니페스토에 등재.
- ⚠️ **날짜 라벨** — 이번 세션은 git 과 라벨이 같다(**2026-08-05**). 같은 날 두 번째 세션.
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.

## Notes for Next Session

- ⛔ **`npm run verify` 는 산장만 돈다.** 다른 사건에 심으려면 `solve-check`(전 사건 파스)
  이나 `build` 를 써라. **심기의 경로를 확인하지 않으면 「심었는데 통과」를 검사 탓으로 읽는다.**
- ⛔ **`generate.ts:906` 의 `record`·결과문은 자리표다** — `:2146` 이 덮어쓴다.
  고쳐도 출력이 안 바뀐다. 주석을 달아뒀다.
- ⛔ **`gen-baseline` 은 `.gen-baseline.json` 이 gitignore 다** — 머신마다 없다.
  **손대기 전에 `save`** 하거나, 이번처럼 `git stash push -- <파일>` 로 옛 코드의
  기준선을 만들고 대조한다.
- ★ **검열관을 돌릴 때는 예상을 먼저 적는다** — 오늘 그 절차가 ②를 「vacuous」로
  정확히 분류하게 해줬다. 예상이 없었으면 「안 걸렸으니 좋다」로 읽었을 것이다.
- ★ **오늘 사용자의 예단이 두 번 반증됐다**(발견자 동일인 · 생성기가 본체).
  **둘 다 지시받은 대조 절차가 잡았다.** 절차가 지시자보다 강하다.
