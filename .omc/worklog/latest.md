---
session_date: "2026-08-04 (git 확인)"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 14단 초록(exit 0) · 커밋 여덟 · 트리 깨끗** (`solve-check.ts` 는 `6d2e660` 에
포함됐다 · **게이트에는 여전히 미배선** — 아래 ⚠).

> ⚠ **이 줄이 「`solve-check.ts` 하나만 미추적」이라고 적혀 있었다 — 적히는 순간
> 거짓이 됐다** (2026-08-05 `before-work` 정정). **b9f49b4 부류(낡은 줄)가 아니라
> 공정의 순서 문제다**: `after-work` 는 **워크로그를 쓴 뒤에 커밋**하므로, 그 커밋이
> 쓸어 담을 파일은 **집필 시점에 언제나 미추적으로 보인다.** 그래서 진짜 수리는 이
> 파일이 아니라 공정이었다 — `.claude/commands/after-work.md` §2 에
> 「미추적이라 적지 말고 **「이 커밋에 포함됨」**으로 적어라」를 박았다.
> **살아 있는 사실은 「게이트에 안 걸었다」쪽이고 그것은 지금도 참이다**
> (`git grep solve-check -- package.json` → 0건).

### ★★ 오늘의 줄기 — 적대적 솔버를 지으려다 **계측기를 얻었다** ★★

챗봇 상담에서 온 `SOLVER-SPEC` 을 검산하고 0·1·3·4단계를 지었다. **판정기로서는
거의 아무것도 변별하지 못했고, 그것이 결론이다** — 세계 논리가 과잉결정돼 있다.

```
전체 정보    discriminated 0/702      셀 것이 없다
0회 시점     범인후보 1 · 세계 245     진술 정독만으로 넷이 배제된다
곡선         평평                     ★ 고장이 아니라 설계의 증명서 ★
```

**사용자 확정**: 이 게임의 과업은 처음부터 「누가」가 아니라 **「무엇으로 증명하나」**다
(예산 안에서 레드 헤링을 피해 확보 단어를 모으는 경제 · `verify` 의 「탐욕 7회 →
여유 -1」이 재는 것이 그것). 그래서 솔버의 관할을 **계약 감시자**로 확정했다 —
축소가 아니다. 무료 배제는 `f_no_*` 넷이 공짜 사실일 때만 성립하고, 생성기가 하나를
빠뜨리면 **격자 모호를 무는 것은 솔버뿐이다.** 뮤테이션 **44/44 물었다.**

### 대차대조 — 나흘의 산출은 판정이 아니다

> ⛔ **다음 세션이 「curve 도 공허했으니 솔버 무용」으로 오독할 자리다.**

| 얻은 것 | |
|---|---|
| 구조 필드 셋 | `Blank.asks` · `World.murderCell` · `Evidence.pointsAt` |
| 실측 하나 | 공란 702개 중 **364개(51.9%)가 산문 읽는 규칙으로만** 검사된다 |
| 무검사 7개 | 전부 손저작(산장) · 워크로그의 「남은 6개」와 **독립으로** 일치 |
| 오배정 적발 2회 | `roleOf` 가 현장 전제 36개 · 사망 칸 30건을 틀렸다 |
| 증명서 | **세계 논리가 과잉결정돼 있다** — 잴 것이 없을 만큼 설계가 조여져 있다 |
| 공정 규칙 둘 | 스펙 §9 — 계측은 `solve()` 경유 · **검산은 다른 데이터 경로** |

**판정기를 지으려다 계측기를 얻었고, 계측이 스키마를 세 번 고쳤다. 손해가 아니다.**

### ① `Blank.asks` — 질문이 산문에만 살고 있었다

`Blank` 이 갖는 것은 `label` 과 `answer` 뿐이고 **무엇을 묻는지는 서술문에만** 있었다.
`proof.ts` 가 산문을 읽어(R1·R3·R6·R7) 우회했고 솔버는 아무 말도 못 했다.

**새 정보가 아니라 파기 중단이다** — 생성기는 만드는 순간 알고 있었다. `generate.ts`
**세 자리**에서 어휘 **열 가지**가 전부 나온다. 생성 640개 커버리지 **100%**.

### ② 실측이 내 계측을 세 번 반증했다

```
① 08-04 손계측 263/439  →  솔버 계산 606/96   답의 *종류*로 갈랐던 것이 틀렸다
② 「산문 전용 14개」      →  착시. RULES 에서 R6 이 R5 보다 앞이라 먼저 잡았을 뿐
③ 「C5~C8 이 28개 못 묶음」→ 36개가 내 역할 오배정이었다 (답이 현장이면 전제다)
```

**③이 아니었으면 스키마를 안 쟀는데 고칠 뻔했다.**

### ③ 그리고 내 도구가 두 번 자기를 속였다 — **아홉·열 번째**

```
C7 초안        세계 0개인데 「답안 벡터 ≤ 1」을 unique 로 셌다 → "39건 unique" 가 "39건 죽었다"
curve 리트머스  기대값을 consistentWorlds(zero) 로 계산 — curve 0회와 **같은 함수**라 언제나 ✓
```

**공통 뿌리: 검산이 피검산과 같은 길을 걸었다.** 스펙 §9 에 규칙으로 박았다.
독립 검산으로 교체 — `k=0 기대 범인 후보 = 5 − (공짜 무죄 사실이 배제하는 수)`
(`c.facts` 배열을 센다). **44/44 일치.**

### ④ C7 폐기 · 스펙 §5 두 번 개정

`foundAt` 은 **장소 참조가 아니다**(44건에서 location id 0/51 · 661개는 필드 없음).
초안을 걸면 **진짜 세계까지 지워 44건 전부 unsat.** 폐기 이유를 스펙에 남겼다 —
다시 지으려 들 자리다.

```
§5 개정 이력: unique(08-03) → curve 끝점(08-04) → 좌항 합성(08-04)
출시 조건 = 기존 게이트(도달성) ∧ 솔버 초록(건전성·계약)
```

### ⑤ 문서 배치 — 매니페스토 조항 + 아침의 어긋남 넷

- **매니페스토 §정보 이중화 원칙 신설** — 「공란이 무엇을 묻는가도 정보다」.
  §어긋나는 세 가지 꼴에 **넷째(D 구조가 자기 뜻을 안 적는다)** 를 더했다
- 어긋남 넷: 게이트 **13→14단**(`NEXT-ACTION` 이 자기 머리말과 모순 · `before-work.md`
  동반) · 검열관 「없다」 **넷** · 상용화 19칸→명령이 정본 · matrix 빈칸 19→숫자 삭제

### ⑥ `prop-check` 이 나를 잡았다

`asks` 를 타입·생성기에만 넣고 `to-yaml`·`schema` 에 안 넣어 왕복이 깨졌다.
**새 필드는 `types + generate + to-yaml + schema` 네 곳이 한 벌이다.**

## In Progress

**`engine/src/solve-check.ts` — 만들었고 게이트에 안 걸었다** (`6d2e660` 에 포함 · **미배선**).

> ⛔⛔ **이 미배선은 의도다.** 지금 `package.json` 에 걸면 **무검사 공란 7개 때문에
> `npm run build` 가 빨개진다.** 계획은 **「초록이 된 뒤 한 배치」**다.
> **그걸 모르고 지금 걸면 → 빨강 → 고치겠다고 §6 교차표 다섯째 줄을 빼는 것이
> 최악의 경로다.** 그 줄이 강등이 만들 뻔한 구멍을 세는 자리다.

현재 상태: ① 무검사 7개 **빨강** · ② 계약 뮤테이션 **44/44 ✓** · ③ 독립 리트머스 **44/44 ✓**

## Remaining Tasks

- [ ] 🎯 **다음 하나 — 산장 서술문 7개를 정독한다 (모든 것에 선행)**
      **결정 1 (사용자)**: 1~5의 「기계적으로 보인다」를 확정하는 것도, 6·7의 어휘를
      정하는 것도 **문장을 읽은 뒤다.** `roleOf` 가 두 번 틀린 원인이 「답만 보고
      질문을 추정」이었고 **손저작에서 같은 걸 반복하면 안 된다.**
      ★ **손저작의 `asks` 는 저작 선언이다** — 추정으로 채우면 솔버가 **틀린 물음 위에서
      증명한다.**
      ```
      # 무검사 7개 — 전부 mountain-lodge. 소지품 고리는 1:1 로 갈린다
      1장 인물   yujin     → belongingsOwner('파스')                  기계적으로 보인다
      1장 인물   yena      → belongingsOwner('지안 소지품 — 연습생 제보')      〃
      2장 인물   wonyoung  → belongingsOwner('리원 문자 — 가명의 정체')        〃
      4장 인물   wonyoung  → 〃                                              〃
      4장 협박대상 yuri     → belongingsOwner('나경 문자 — 협박')  ★소지품이 곧 협박문자★
      2장 마지막목격자 sakura → 🔴 어휘에 없다 (결정 2)
      2장 장소   main      → 🔴 어휘에 없다 · scene 은 room 이라 다르다 (결정 3)
      ```
- [ ] **결정 2 (사용자) — `#6 lastSeenBy` 조건부 승인.** 어휘 확장 승인.
      격자에서 도출되는 **세계 변수**라 **첫 `discriminated` 공란**이 되는 것이 이 어휘의
      가치다. **조건: `answerOf` 의미론을 정확히 박을 것** —
      *"`murderCell` 직전, 피해자와 현장에 같이 있던 사람"*.
      ⚠ **그 칸에 둘 이상이면 답이 세계의 함수가 아니다** → `undecidable` 로 정직하게
      뱉게 둔다(C9 가 원래 그 일을 한다). 세계마다 범인 동선이 다르니 갈릴 수 있고,
      **갈리면 `discriminated` — 그게 정상 동작이다.**
- [ ] **결정 3 (사용자) — `#7` 은 서술문 없이 결정 금지.** 지금 어휘를 고르는 것이
      정확히 오늘 두 번 밟은 함정이다. **예단 하나만 적어둔다**: #6과 짝일 가능성 —
      *"누가 마지막으로 봤나(#6) / 어디서(#7)"* 라면 `lastSeenLoc` 으로 한 쌍이고
      **의미론도 #6에 종속**된다(그 목격이 일어난 장소). **문장이 다른 걸 묻고 있으면 그때 다시.**
- [ ] **`asks` 62개** — 생성분은 서식이 보장하므로 **손저작 7개 외 55개는 기계적**
- [ ] **`pointsAt` 방어 검사** — 손저작이 `pointsAt: hall` 을 적고 record 에는 별채를
      가리키는 문장을 쓰면 **솔버가 틀린 사실 위에서 증명한다.** record 문장 안에
      `pointsAt` 장소의 라벨이 실재하는지 검사 — **검열관 ⓐ(인용 실재)와 같은 모양**이라 싸다
- [ ] **`solve-check` 배선 + `matrix.ts` 도출 축 등록** — 한 배치로. 배치가 닫히면
      **4단계 계획 1번(솔버)이 게이트까지 포함해 완결**이다
- [ ] **📄 문서 — 4단계 표에 ⏭ 스냅샷 배너 (사용자 결정 2026-08-05 · `after-work` 에서 실행)**
      `NEXT-ACTION.md:2626` 의 *"③ 검열관 … LLM 쪽은 아직 없다"* 가 ⬜ 로 남아 있다 —
      검열관은 **08-02에 닫혔다**(`censor.ts`·`CENSOR-BRIEF.md`·게이트 `censor-check`).
      ⛔ **표를 현재 상태로 갱신하는 쪽은 기각됐다** — 날짜 절(07-28) 안의 표를 계속
      고쳐 쓰면 **「낡은 표가 살아 있는 척」을 상설화**한다. `PORT-AUDIT.md` 전례대로
      **⏭ 배너**를 달고, **「지금 위치」의 정본은 `NEXT-ACTION` 머리말로 일원화**한다.
      ★ **`.claude/commands/before-work.md` §5 의 「①산문을 스키마로/②산문가/③검열관/
      ④campaign 작가」 지시문도 머리말을 가리키게 고치는 것까지가 이 수리의 완결이다** —
      지시문을 안 고치면 다음 세션이 **또 저 표로 간다.**
- [ ] 🔴 **테스터 피드백 0건** (사용자 결정으로 접어둠) · **용의자 프로필 채널 43/44 빔**
- [ ] 동선 모양 경고 둘 · `prose-lock` 「아침의 정황」 · 여덟째 아키타입 · `es-hangul`
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌 · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **`matrix.ts:158` 의 `PL = 'src/prose-lock.ts'` 가 죽어 있다** — 린트 경고 1건의
  정체. `CS`(censor)는 5칸이 쓰는데 `PL` 은 **어느 칸도 안 가리킨다.** `prose-lock` 이
  게이트 한 단인데 행렬이 안 센다. **`solve-check` 등록할 때 같이 판단한다.**
- ⚠️ **`solve-check` 미배선이 의도다** — 위 §In Progress 의 ⛔⛔ 를 반드시 읽는다.
- ⚠️ **손저작 4건은 `asks` 가 없어 솔버가 `undecidable` 을 낸다** — 결함이 아니라
  **설계다**(`roleOf` 폴백을 일부러 안 남겼다). 채우면 풀린다.
- ⚠️ **날짜 라벨** — 이번 세션은 git 과 라벨이 같다(**2026-08-04**).
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.

## Notes for Next Session

- ★★ **「짓기 전에 잰다」가 오늘 다섯 번 값을 했다** ★★ C7(스키마 고칠 뻔) · 역할
  오배정 36개 · 「산문 전용 14」 착시 · 세계 확장 기각 · 곡선 평평의 원인.
  **반대로 재는 도구가 두 번 고장났고 둘 다 「검산이 피검산과 같은 길」이었다.**
- ★ **외부 스펙을 받으면 인용부터 못 믿는다** — `space-audit.ts` 가 「이미 있음」이라
  적혀 있었으나 **우리 트리에 없었다.** 숫자는 다시 재서 전건 맞았다. **순서가 중요하다** —
  숫자가 맞아서 인용을 봐준 게 아니라 인용이 안 맞아서 다시 쟀더니 맞았다.
- ★ **안 쓰는 것은 거짓말이다 — 오늘 세 번** `PL` 상수 · `answerOf` 의 `Case` 인자 ·
  `guiltyUnder` 헬퍼. **그리고 반대 방향도 같다** — `observed?` 를 옵션으로 두면
  「안 넘겨도 되는 인자」가 되어 곡선이 평평한 거짓말이 될 수 있었다. **필수 인자로 했다.**
- ⛔ **`generateCase` 에서 throw 하지 마라** — 브라우저가 부른다. `solver.ts` 도 같은 규약.

## Files Modified

```
engine/src/solver.ts        ★신규★ 세계 열거 · C1~C4 · asks/answerOf · curve · 지식 조항
engine/src/space-audit.ts   ★신규★ 산정 재현 (챗봇 인계분 · 게이트에 안 건다)
engine/src/solve-check.ts   ★신규★ 게이트 15단 후보 — 초록 뒤 배선 (미배선)
engine/src/types.ts         Blank.asks · Evidence.pointsAt 신설
engine/src/generate.ts      asks 를 세 자리에서 방출 · 가닥 물증에 pointsAt
engine/src/verifier.ts      availableActions·simulate export · simulate 에 trace 추가
engine/src/to-yaml.ts · schema.ts   asks · pointsAt 왕복
docs/SOLVER-SPEC.md         ★신규★ v2 + 실측 개정 (C7 폐기 · §5 두 번 · §9 공정 규칙 둘)
docs/MANIFESTO.md           §정보 이중화 원칙 신설 · 어긋남 넷 중 셋
docs/MEMORY.md · NEXT-ACTION.md · SESSION-LOG.md · .claude/commands/before-work.md
package.json · engine/package.json   space-audit 스크립트
.omc/worklog/latest.md      이 파일
```
