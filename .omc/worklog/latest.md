---
session_date: "2026-08-02 오후~밤 (git 확인 · 진짜 08-02)"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 14단 초록(exit 0) · 커밋 열여섯 · 트리 깨끗.**

> ⛳ **날짜 주의 — 이번엔 방향이 반대였다.** 직전 세션이 스스로를 「2026-08-01 밤」으로
> 라벨했는데 **커밋 16개 중 10개가 git 08-02** 였다(자정을 넘겼다). 대응표는
> *"라벨이 git 보다 하루 **앞선다**"* 고 적어뒀는데 그 세션은 **하루 뒤처졌다.**
> **이번 세션은 git 을 따라 08-02 로 적는다** — 다만 문서의 옛 「2026-08-02」 절은
> 여전히 **git 08-01**(clues.ts·게이트 12단)이니 **기계·시각으로 갈라 읽는다.**

### ★★ 오늘의 줄기 — **상용화 기준을 못 박고 그 기준을 채웠다** ★★

사용자 질문이 방향을 정했다 — *"작업이 계속 늘어나는 느낌이다. 늪에 빠진 느낌인데,
상용화할 수준이 되려면 몇 %까지 올려야 하나(재미 같은 주관 축 말고)."*

원인을 찾았다: **행렬 전체 % 를 출시 기준으로 읽고 있었다.** 그건 「검사할 수 있는
표면 중 얼마나 검사하나」지 「팔 수 있나」가 아니다. 축별로 갈라 세니 드러났다:

```
배선  11/13  85%   가리키는 게 실재하나
도출   2/ 4  50%   proof-check 320/320 이 실측으로 받친다
모순   3/14  21%
누설   2/13  15%   ← 상품을 죽이는 축인데 제일 낮았다
```

**41% 가 낮은 게 문제가 아니라 낮은 자리가 하필 상품 그 자체인 게 문제였다.**
그래서 **상용화 게이트 = 누설 축 전부 + 산문 채널의 모순 = 18칸**으로 못 박고,
`matrix.ts` 의 `inShipGate` 가 **규칙으로** 정하게 했다(태그로 달면 새 채널에서 빠진다).

```
3 / 19  →  18 / 18      자동 13 + 브리프 5 · 남은 0
```

### ① 침묵 53개 → 0 · `mute-check` 55% → 100%

무리 셋이 **같은 모양의 구멍**이었다 — **검사가 죽은 게 아니라 도구가 그 자리까지
못 갔다.** 원인 셋:

| | |
|---|---|
| 파손이 거칠다 | 평면도·관계도를 「통째로 지운다」뿐이라 `if (c.floorPlan)` **안쪽이 실행조차 안 됐다** |
| 「전부」가 균일을 만든다 | §9-14·§6.56 은 **「섞여 있음」**을 무는데 전부 지우면 **오히려 조용해진다** |
| 닻이 코드였다 | `${}` 안 중첩 백틱에서 정규식이 잘려 **어떤 메시지에도 없는 글자**가 닻이었다 |

★ **먼저 세라던 ⓐ(스키마가 본다 → 사거리 밖)는 빈 범주였다 — 0개다.**
mute-check 는 파싱 **뒤**를 망가뜨리므로 schema 가 안 돌고, **생성 사건은 `parseCase`
를 아예 안 거친다.** 문서가 「그다음 배치」로 미뤄둔 넷이 정확히 스키마와 겹치는
넷인데 **첫 시도에 전부 울렸다.** 옛 헛짚음은 **죽은 뮤테이션**에 대한 것이었는데
그 교훈이 **침묵한 자리** 쪽 축으로 옮겨 적혀 있었다.

### ② 검사 다섯 종 신설 — §9-15 ~ §9-19

| | |
|---|---|
| **§9-15** 진술 산문 × 모순 | 행렬의 「가장 큰 빈칸」. 거짓 양성 0/16 · 심어서 70/70 |
| **§9-16** 인물별 수량이 범인을 혼자 세운다 | 프로필 칸 수 · 조사 개수 · 비용 합. §9-3h 의 확장 |
| **§9-17** 추가 진술 × 모순·누설 | §6.9(내용 안 봄)와 §9-7(결과문까지) **사이로 빠져** 있었다 |
| **§9-18** 프롤로그 × 누설·모순 | 범인만 부르나 · 죽은 이가 피해자인가 · **인원수를 글자로 쥔 자리** |
| **§9-19** 조사 결과문·물증 기록이 범인만 부른다 | 열린 경계(*"다른 방식"*)를 **명제 둘로 닫았다** |

★ **§9-15·§9-17 의 명제가 같다** — **진실이 아니라 「그 사람이 하는 말」과 맞춘다.**
범인은 격자에서도 거짓말하고(`claim`) 산문이 **그것과 일치하는 것이 정상**이다.
진실과 대조하는 판을 먼저 심어봐서 알았다(「안 물림」 11건이 전부 정답이었다).

### ③ 도구·장부의 자기기만을 여덟 번 잡았다

```
mute-check 가 침묵 목록을 30개에서 잘라 숨겼다        그 목록이 곧 할 일인데
mute-check 의 표본이 생성 사건뿐이었다               slice(0,8) · 저작 YAML 을 한 번도 안 망가뜨렸다
뮤테이션 하나가 이름과 다른 것을 쟀다                person·location 은 없는 필드다
행렬이 §9-3h(오류 등급)를 모르고 「안 본다」고 적었다   다섯 번째
행렬이 report×누설의 답을 적어놓고 「안 본다」고 했다   여섯 번째
행렬이 죽은 검사(§6.8)를 표식으로 썼다               일곱 번째 — 주석만 남아도 표식 검사는 통과한다
행렬이 「양방향으로 문다」면서 한 방향만 물었다         ← 역방향을 기계로 세웠다
내 계측이 자기 버그를 가려 0건을 인쇄했다            who.includes('') 가 언제나 참이었다
```

★ **여덟 번째가 제일 무섭다** — *"재보고 0 이 나왔다"* 고 안심했는데 **재는 쪽이
고장나 있었다.** 잡은 건 게이트의 `npm run case` 였다.

### ④ `prose-lock` — 생성 경로의 ⓐ 벽을 검열관 없이 닫는다 (게이트 13단째)

생성기가 쥔 서술 문장이 **21종뿐**이고 씨앗 50건에서 포화한다(400까지 그대로).
**목록을 잠그고 늘면 사람이 읽는다.** ⚠ **인물 이름을 정규화**해야 한다 — 안 그러면
팔레트가 이름을 바꿀 때 서식이 그대로인데 잠금이 깨진다.
★ 심어서 확인 — 07-30의 「그날 아침, 가장 먼저 일어난 사람이…」를 넣으니 exit 1.

### ⑤ ⑤검열관 — `MANIFESTO §남은 일 ②` 가 닫혔다 (게이트 14단째)

미루라며 단 조건이 충족됐다 — *"①이 끝나면 **②가 정말 필요한 자리가 몇 개인지
드러난다**."* **답은 5칸이었고 그 5칸만 겨냥해 지었다.**

★ **값은 「판정」에 있다** ★ LLM 이 낸 경고를 **그대로 믿지 않는다**:

```
ⓐ 인용한 문장이 실제로 그 자리에 있나   없으면 지어낸 것 → 기각   ← 환각 차단
ⓑ 가리킨 자리가 실재하나 · ⓒ 등급이 아는 것인가          → 기각
```

**ⓐ가 없으면 §14 「LLM 은 규칙을 결정하지 않는다」가 말뿐이 된다.**
`censor-check` 이 그 배선을 게이트에서 문다(끊으니 exit 1).

### ⑥ 브리프 5칸을 자동 13칸과 **같은 ✓ 로 안 셌다**

행렬에 `brief`(◑) 등급을 새로 뒀다. 사람 왕복이 필요한 반자동이라 같은 칸으로 세면
**이 표가 자기를 틀리게 말하는 아홉 번째**가 된다. 머리 숫자·게이트 숫자 둘 다 갈라 인쇄한다.

## In Progress

없다. 게이트 초록 · 트리 깨끗 · 푸시 완료에서 멈췄다.

## Remaining Tasks

- [ ] 🎯 **다음 하나 — 검열관을 실제로 한 번 돌린다 (`closing-theater`)**
      ```bash
      npm run censor -- cases/closing-theater.yaml          # 브리프가 나온다
      npm run censor -- cases/closing-theater.yaml --judge resp.yaml
      ```
      `closing-theater` 는 **프롤로그 불일치를 일부러 남긴** 사건이다(아래 Blockers).
      **그것을 잡으면 파이프라인이 증명된다.**
      ★ **사용자 손이 꼭 필요한 건 아니다** — 「내가 저작할 때 LLM 을 안 쓰는 게
      아니다」와 같은 자리다. **내가 검열관 역을 하면 된다**: 브리프를 읽고 응답
      YAML 을 써서 `--judge` 에 먹인다. 사용자의 챗봇은 **유저용 경로**다.
      ⚠ **판정이 기각하면 그것도 결과다** — 내 경고가 환각이었다는 뜻이다.
- [ ] 🔴 **테스터가 뭐라고 했는지를 받아 적는다 — `docs/PLAYTEST.md` 신설**
      **코드가 못 재는 ①축(재미·난이도 체감)은 전부 여기서만 온다.**
      ⛳ 사용자 결정으로 접어뒀다. 한 판이라도 얘기가 들어오면 그때가 이것의 차례다.
- [ ] 🔶 **용의자 프로필 채널이 43/44 사건에서 비어 있다**
      `action.clues` 를 가진 사건은 **산장 하나뿐**(12개)이고 생성 40 + 저작 3 이 전부 0.
      `generate.ts` 가 `clues` 를 안 만든다. **누설이 아니라 결손**이라 게이트에 안 넣었다 —
      관측 채널 하나가 통째로 죽어 있다(`MEMORY §9-8` 의 재발).
- [ ] **`mountain-lodge`·`practice-room` 의 동선 모양 경고 둘** — §9-11 을 `template`
      게이트에서 풀면서 새로 떴다. **결함 보고이지 거짓 양성이 아니다** — 범인의 동선이
      다섯 중 유일해서 격자 표에서 혼자 다른 줄이 된다. **저작 판단이 필요하다.**
- [ ] **`prose-lock` 의 「아침의 정황」 한 줄** — 21종 중 유일하게 시간을 주장한다.
      지금은 1장이 아침 정황이라 참이지만 **장 구성이 바뀌면 거짓이 된다.**
- [ ] **`mountain-lodge` 의 남은 6개** — 손으로 쓴 산문이고 근거가 진술 안에 있다.
      **규칙이 못 읽을 뿐**이라 산문을 규칙에 맞추면 개악이다.
      ⚠ **코드에 못 박혀 있지 않다** — 7번째가 생기면 구별이 안 된다(허용목록이 없다).
- [ ] **여덟째 아키타입** · **§번호 → 행렬 좌표 개명** · `es-hangul` 이관
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌 · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **테스터 피드백이 저장소에 0건이다** — 사용자 결정으로 접어뒀다.
- ⚠️ **`closing-theater` 는 프롤로그 불일치를 일부러 남긴 사건** — 테스터 목록에서
  뺀다. **그리고 그것이 검열관의 재현 테스트다.**
- ⚠️ **커밋된 4건 경고가 13 → 15 로 늘었다** — 늘어난 둘은 §9-11 을 푼 결과이고
  **진짜 발견**이다(위 Remaining 참조). 거짓 양성이 아니다.
- ⚠️ **`mute-check` 는 `schema.ts` 를 못 본다** — 사거리 밖이다. 다만 **그것을
  「스키마가 보는 자리는 침묵해도 된다」로 넓혀 읽으면 안 된다**(08-02에 0개로 확인).
- ⚠️ **행렬의 표식 검사는 「글자가 있나」만 본다** — 주석만 남은 죽은 검사도 통과한다
  (§6.8 이 그랬다). 역방향 검사로도 못 잡는다. **도구의 사거리로 적어뒀다.**
- ⚠️ **`R8` 을 `R4` 앞으로 옮기면 조용히 망가진다** — 주석에 박아뒀다.
- ⚠️ **날짜 라벨** — 직전 세션은 git 보다 **뒤처졌고**(자정 넘김) 그전엔 **앞섰다.**
  **앞으로도 git 을 따른다.** 과거 라벨은 안 고친다.
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.

## Notes for Next Session

- ★★ **「짓기 전에 잰다」가 오늘 세션 하나를 아꼈다** ★★ 「보고서 서술문 × 모순」을
  다음 타로 잡았다가 세 번 재보고 접었다(장 단위 45/45 거짓 양성 · 문장 단위 0/487 ·
  글자에 이름표 0/1037). **짓고 나서 알았으면 거기서부터 명제를 고치기 시작했을 것이다.**
- ★ **기준선 없는 %는 뜻이 없다** — salience 고립 34%가 커 보였는데 무고한 자가 41%였다
  (배율 0.82x). **범인이 오히려 덜 튄다.** 기준선을 안 댔으면 없는 누설을 잡으러 갔다.
- ★ **채널마다 ⓒ(닻 대조)가 되는 정도가 다르다** — 추가 진술 64% · 프롤로그 47% ·
  장전환 1% · 보고서·인터루드 0%. **「전부 같은 기법으로 간다」고 가정하면 안 된다.**
- ★ **거짓 양성의 두 단골** — ⓐ 자기 대상 이름(「소지품 검사 · 문세라」) · ⓑ 합성명사
  (서식의 `인물 하나` 때문에 「물건 **하나**가」가 걸렸다). **별명꼴은 1인칭 진술에서만 쓴다.**
- ★ **문서의 숫자를 블랭킷 치환하지 마라** — 「12단→13단」을 일괄로 바꿨더니 **과거
  세션 기록까지** 바뀌었다. 그건 이력이다. 되돌리고 살아 있는 줄만 손으로 고쳤다.
- ⛔ **`generateCase` 에서 throw 하지 마라** — 브라우저가 부르므로 유저 화면이 빈다.

## Files Modified

```
engine/src/mute-check.ts        뮤테이션 61 → 109종 · 닻 추출 재작성 · 표본 확대 · 자르기 제거
engine/src/verifier.ts          §9-15·§9-16·§9-17·§9-18·§9-19 신설 · §9-11 template 게이트 분리
engine/src/matrix.ts            ★역방향 검사★ · 상용화 게이트(inShipGate) · brief 등급 · 정정 7건
engine/src/prose-lock.ts        ★신규★ 생성기 서술 문장 21종 잠금 (게이트 13단째)
engine/src/censor.ts            ★신규★ ⑤검열관 — 브리프 생성 + 판정(환각 차단)
engine/src/censor-check.ts      ★신규★ 검열 배선 검사 (게이트 14단째)
engine/templates/CENSOR-BRIEF.md ★신규★ 검열 의뢰 서식
package.json · engine/package.json  게이트 12단 → 14단 (prose-lock · censor-check)
docs/NEXT-ACTION.md · MEMORY.md · SESSION-LOG.md
.claude/commands/before-work.md  게이트 13단 표기
.omc/worklog/latest.md          이 파일
```
