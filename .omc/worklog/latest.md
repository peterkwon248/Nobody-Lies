---
session_date: "2026-08-06"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kwonkyunghun/Desktop/Nobody Lies"
machine: "DESKTOP-JCJTAH8"
---

## Completed Work

**게이트 15단 → 16단 exit 0 · 커밋 열셋 · 배포 다섯 번 READY · 트리 깨끗.**

⚠ **머신이 바뀌었다** — 직전은 `KWONKYUNGHUN`(OneDrive), 이번은 `DESKTOP-JCJTAH8`(Desktop).
`npm install` 은 필요 없었다(워크스페이스 호이스팅으로 루트에 다 있었다).

### ★★ 이 세션의 줄기 — 테스터 한 명이 게이트가 못 본 것을 네 번 찾았다 ★★

전찬웅(아이폰 사파리)이 배포본을 네 차례 플레이하며 보고했고, **그때마다 게이트
열여섯 단이 초록인 채로 살아 있던 결함이 나왔다.**

```
1차  "시각 장소 인물 다 잘못들어간듯"  → 닫힌 공란 71/99 에 정답이 없었다 (3주 묵음)
3차  "집은 기깔나게 지어놨는데 텅텅"   → 관측 채널 파기 3 · 결손 2 · 발견성 1
4차  "엔딩이 답만 알려주고 띡"        → 해설을 3막으로
5차  "여전히 딱딱한 진실 나열"        → 측정 배치 (동기가 명사구 하나)
```

**게이트는 「엔진 데이터 안에서 도출 가능한가」를 증명했고 그것은 참이었다. 그런데
앱이 그 채널을 화면에 안 내보내면 플레이어에게는 없는 것과 같다.**

### 결산

```
                     전        후
닫힌 공란 정답 없음   71/99      0/99
게이트                15단       16단   cand-check §1 정합 · §2 배선 · §3 관측 표면
관측 채널 파기         3          0     물증 record · 증명 사슬 · (salience 는 기각)
관측 채널 결손         2          0     에필로그 · (프로필 clues 는 저작 큐)
조사 화면 도달         불가       가능   사이드바 + 더보기 시트 (양 폭)
해설                   없음      3막    재구성 · 사슬 · 모범 수사 대조
인물 후보              6          5     피해자 제거 (실사용 0/52)
```

### 지은 것

- **`cand-check`** (게이트 16단째) — §1 정합(정답이 후보에 있나) · §2 배선(사건
  데이터성 전역을 `applyCase` 가 갈아끼우나) · §3 관측 표면(파생 채널이 실려 나가나).
  뮤테이션 전건 확인
- **`engine/src/epilogue.ts`** — 트릭 재구성 방출 + `ACT1_TEMPLATES`(1막 문장틀, 잠정)
- **`engine/src/emit-derived.ts`** — 파생 필드를 **한 곳**에서. 나가는 문이 둘이라서
- **`verifier.oraclePath()`** — 모범 수사 단계별 경로(오라클 최단)
- **`docs/ACT1-MEASUREMENT.md`** — 1막 심화 측정 보고(초안의 입력)

### ⛔ 밟은 자국

```
① 나가는 문이 둘      export-case 만 고쳐 생성 사건 해설이 빌 뻔 → cand-check §3 이 잡음
② 같은 자리 두 번     감사 #1 때 log 만 넣고 investigate 를 안 봄 → 어느 폭에서도 도달 불가
③ 계수기 다섯 번      읽기를 대입으로 셈 · l.name/l.label · 객체↔문자열 · 크기를 c 로 잼
                      · 배포 확인을 옛 기준값과 비교
④ 검사가 배포를 죽임   cand-check 이 gitignore 산출물을 전제 없이 읽었다 (그리고 그게 옳았다)
```

### ★★ 전제가 두 번 반증됐다 — 둘 다 짓기 전에 쟀다 ★★

```
"salience 를 등급으로 켜라"   → salience 는 유망도가 아니다. ≥0.65 는 전부 redherring
                               (verifier:407). 켜면 유도가 반대로 돌고 3단으로 가르면
                               「중간 = 정답」이 노출된다 → 안 지었다
"simulate() 오라클 경로"      → 그 함수 주석이 스스로 부정("최단 경로가 아니라 이쪽")
                               미끼 밟는 길을 「모범」이라 부를 뻔 → findMinPath 로 갈았다
```

## Blockers

없다. 게이트 초록 · 배포 READY.

## ⛔ 전달 구간 증발 — 다섯 번

측정 보고를 채팅으로 다섯 번 보냈고 다섯 번 다 **빈 문서**로 도착했다(긴 본문이
「붙여넣은 텍스트」 첨부로 자동 변환되며 내용이 떨어졌다). 평문화·압축도 실패.
→ 경훈 판단으로 **매체를 바꿨다**: `docs/ACT1-MEASUREMENT.md` 로 커밋 → GitHub 에서 읽기.

★ 지시서 증발(`bef1afd`) 때는 **경로 대신 내용**이 답이었는데, 이번엔 **내용조차 못
건너서 매체를 바꿨다.** **긴 산출물은 처음부터 저장소로 보낸다.**

## Next Steps

**1막 초안 대기.** 입력은 `docs/ACT1-MEASUREMENT.md`. 초안이 낼 것 셋 —
비트 틀(척추/곁가지) · 동기 풀 4항목 × 3칸 · 예시 팔레트 3개 × 4항목.
초안 후: 틀·풀 교체 → `cand-check §3` → 10건 눈검사 → 게이트 → 배포 → 찬웅 재전송.

## Watch Out

- **산장 1막에 「사건은 여기서 일어났다」가 두 번** — 범인이 현장에 두 칸 머물면 매 칸에
  붙는다. 범행 시각은 거짓말이 난 칸 하나. **1막 재작성 때 같이 닫는다**
- **`motive` 는 공란의 정답이자 확보 단어**(`generate.ts:2981`·`:3000`) — 풀을 문장으로
  바꾸면 드롭다운·단어 은행이 깨진다. `label` 을 반드시 남긴다
- **떡밥 회수의 초반이 비어 있다** — 동기 낱말이 진술에 0/10
- 상단 크롬 두 겹(`.tabbar` 44 + `.viewheader` 48) — 겹침만 끊었고 뿌리는 구조 백로그
- 프로필 clues 는 **결손**이라 임시로 조사 결과문을 물려뒀다 — 저작이 끝나면 걷어낸다
