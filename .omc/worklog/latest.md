---
session_date: "2026-07-31"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 9단 초록(exit 0) · 생성 300/300 통과 · port-check 308/320/12/어긋남 0.**
커밋 넷: `4f654b3`(다양성 셋) · `5291859`(NUL) · `b6cacec`(단일 출처) · 이 인계.

> ★★ **사용자가 개념 하나를 바로잡았다 — 「생성기 = 유저용」** ★★
>
> *"클로드코드로 디테일한 캠페인을 만들 땐 생성기가 꼭 필요한 게 아니잖아.
> 내가 너한테 요청할 땐 생성기를 안 써도 된다는 거임."*
>
> `MANIFESTO.md` 에 **§사건이 생기는 길은 셋이다**(손저작 · 생성기 · 들여오기)를 세워
> 정본으로 삼았다. **구분은 이미 코드에 있었다** — 게이트가 `gen-check`(생성기)와
> `tmpl-check`(손저작 서식)를 별개 단으로 갖고, 커밋된 4건 중 **둘이 생성기를 안 거쳤다.**
> **새 결정이 아니라 `design-brief.md:442` 의 묻혔던 결정**이었다.

- **낡은 문서 셋** — `MANIFESTO §미이행`(§9 수정 브리프는 07-31 밤에 이미 지어졌다) ·
  `MEMORY.md` 의 **「낡은 다음 하나」를 경고하는 문단이 스스로 그 부류였던 자리** ·
  `before-work` 7단→9단. 인용 출처도 정정(`SYSTEM-DECISIONS.md` → `design-brief.md:442`).
- ★★ **도면 다양성 셋** ★★ (300건 · `npm run plan-check`)
  ```
                전                    후
  현장 자리      4칸 · 두 칸에 90% →  7칸 · 상위 두 칸 56%
  방 수         6칸 99.7%         →  5칸 99 · 6칸 117 · 7칸 84
  빈 자리        0 (불가능했다)     →  47% (노치 126 · 중정 15)
  최소 방 폭      130               →  130      ← 안 좁아졌다
  통과율         100%              →  100%
  ```
  - **① 렌더러** §sWalls 는 **덮인 횟수 ≥2** 인 구간만 내벽, §sPoche 는 **방들의 합집합**
    (`martinez`). 중정=구멍 링 · ㄱ자=꺾인 외곽선. **막고 있던 것이 렌더러 하나였다**
  - **② 현장 자리** 문제가 「크기」가 아니라 「자리」였다. **사용자 결정으로 1~2위 중에서**
  - **③ 방 개수** 출처가 `chapters` 뿐인데 **표준 경로가 안 넘겨 언제나 5**였다. 딸린 방 규모±1
- **`npm run plan-check` 신설** — 게이트에는 안 건다(기준치가 없어 초록의 뜻이 없다).
- **검증기 §9-3i 문구 수정** — ⓒ가 그날 거짓이 됐다. 안 고치면 **없는 결함을 예고한다.**

### 세션 후반

- **`world-check.mjs` 의 NUL 두 개**(`5291859`) — `.join()` 구분자가 소스에 날것으로 박혀
  **ripgrep 이 파일을 통째로 건너뛰었다.** `'\x1f'` 로 교체. ★ 고친 뒤 **그 위에서 센
  값이 오염됐는지 되짚었다** — `2.46` 은 이 파일에 없어 「문서 밖 열 곳」은 그대로 맞다.
- **①(도출로 바꾸기)을 시작하려고 먼저 셌더니 예상과 달랐다** — 생성기가 지어내는 문장은
  **report 18 · narration 3 · opening 3 이 전부**였고 **A 부류는 거의 닫혀 있었다**.
- **대신 「한 값이 여러 곳에」가 재발**(`b6cacec`) — `e_mutual.record` ≡ `a_alibi` 결과문,
  `e_tool.record` ≡ `terms[도구].note`. 보이는 자리는 두고 `REC_TOOL`·`REC_MUTUAL` 로
  출처만 하나로. 출력 무변화는 사건을 뽑아 확인했다.

## In Progress

없다. 게이트 초록에서 멈췄다.

## Remaining Tasks

- [ ] 🎯 **테스터에게 URL 을 보낸다** — **사용자 손.** 세 세션째 열려 있다.
      ⚠ **커밋된 4건은 다양성 작업 이전에 뽑힌 것**이라 새 도면을 보여주려면 다시 뽑아야 한다
- [ ] 🎯 **코드 쪽 다음 하나 — 서식이 숫자를 `voice-ref.json` 에서 받게 한다**
      (`MANIFESTO §남은 일` ①의 "서식도 같이" + ③의 남은 반쪽. **두 항목이 같이 닫힌다**)
      `2.46` 이 `PROSE-BRIEF.md` 2곳 · `STATEMENT-BRIEF.md` 5곳 — **서식은 프롬프트다**
      - ⛳ **먼저 사용자 결정**: `.md` 에 `{{ratio}}` 치환을 넣을지(단일 출처 · 파일 단독
        가독성 잃음), **`brief-check` 만** 달아 어긋날 때 걸리게 할지(파일은 그대로 읽힘)
- [ ] ①의 나머지 — **basis 구조**로 바꾸기
- [ ] 사이드바 항목 접근성(`role`·`tabindex` 없는 `div`) — 별건으로 띄워뒀다

## Blockers / Issues

- ⚠️ **`MIN_W` 가 자기가 안 지키는 값을 말한다** — 150인데 실제 바닥은 **130**
  (`bar` 기본 배분 `[190,150,130,130]` + `jitter` 바닥이 `Math.min`).
  *"가드가 조용히 포기"* 의 재발이고 **아직 살아 있다.** 결함으로 보진 않았다 —
  130에서 이름이 한 줄로 들어가는 것을 화면에서 확인했다. `plan-check` 가 매번 인쇄한다.
- ⚠️ **사이드바 항목이 키보드·스크린리더로 안 잡힌다** — a11y 트리에서
  「진술·현장·메모」가 통째로 빠져 자동 도구로도 못 눌렀다.
- ⚠️ `closing-theater` 는 **프롤로그 불일치를 일부러 남긴** 사건 — 테스터 목록에서 뺀다.
- ⚠️ 끝낸 사건도 홈에서 「진행 중」으로 보인다.
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.
- ✅ ~~`world-check.mjs` NUL~~ — 닫혔다(`5291859`).

## Notes for Next Session

- ⛔ **⑤검열관을 먼저 짓지 않는다.** 세션 중에 *"다음 큰 것은 ⑤검열관"* 이라 말했다가
  정정했다 — `MANIFESTO` 가 *"②를 먼저 짓지 않는다. ①이 끝나면 검열할 것 자체가
  줄어든다"* 고 명시한다. **①이 안 끝났다.**
- ⛳ **합치지 않고 남긴 두 쌍** — `a_room`↔`e_tool`, `a_body`↔`e_toolmark` 는 같은 사실을
  다른 문장으로 말하는데 **일부러 쓴 짝**이다(`record` 가 조사 제목의 문장꼴이고, 결과문은
  발견 순간이라 장소를 포함한다). §이식 규칙 6 의 *"안 서면 묻는다"* 를 적용한 자리다.
- **계측을 짤 때 `generateCase` 만 부르면 전건이 예산 오류로 떨어진다** —
  예산은 **실험자(`orchestrate.run`)** 가 정한다. 「200/200 실패」를 보고서야 알았다.
- **`npm run verify -w engine -- --generate N --emit <dir>` 는 npm 이 인자를 삼킨다** —
  `npx tsx engine/src/cli.ts --generate N --emit <dir>` 로 직접 부른다.
- **검색이 거짓말을 하고 있었다면 그 위에서 센 것도 의심한다** — NUL 을 고친 뒤 실제로 되짚었다.
- 처음 렌더러 판본이 **테두리를 두 번 그렸는데 색이 같아 화면에서 안 보였다.**
  **「안 보인다」가 「없다」가 아니다.**

## Files Modified

```
docs/MANIFESTO.md               §사건이 생기는 길은 셋이다 신설 · §미이행 닫음 · §남은 일 재측정
docs/MEMORY.md                  이번 세션 절(후반 포함) · :103 낡은 스냅샷 삭제 ·
                                오케스트레이터 범위 배너 · 732→2,334줄 · 인용 출처 정정
docs/NEXT-ACTION.md             머리말 · §다음 즉시 액션 · 다양성 ①②③ 닫음 · 도구 셋→넷
docs/SESSION-LOG.md             이번 세션 절(최신이 위)
.claude/commands/before-work.md 게이트 7단 → 9단
app/src/App.jsx                 §sWalls(덮인 횟수) · §sPoche(합집합 · martinez)
engine/src/generate.ts          §빈 자리 · 현장 1~2위 · 딸린 방 규모±1 · REC_TOOL·REC_MUTUAL
engine/src/verifier.ts          §9-3i 뿌리 주석 · ⓒ 문구
engine/src/plan-check.ts        ★신규★ 도면 다양성 계측
scripts/world-check.mjs         NUL 두 개 → '\x1f'
package.json · engine/package.json   plan-check 스크립트
```
