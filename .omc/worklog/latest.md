---
session_date: "2026-07-29"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN (OneDrive 아래)"
---

## Completed Work

**작업 트리 깨끗 · 빌드 게이트 7단 초록(exit 0) · 콘솔 에러 0.**

> ⚠️ **다음 세션은 다른 기계다** (사용자 예고). 여기 적힌 것만 넘어간다.

### 이번 세션이 닫은 것 — 생성기가 「막힘」에서 「얇음」으로 내려왔다

```
✅ ① 산문을 스키마로
🔨 ② 산문가        조립 진술에 말투·비밀이 붙었다 · 2차 브리프가 프롤로그까지 받는다
⬜ ③ 검열관        규칙 쪽 §9-10 추가. LLM 쪽은 여전히 없다
✅ ④ 작가
```

### 묶음 1 — 문서가 자기 자신과 어긋나 있었다 (넷)

1. **`MEMORY.md` §오케스트레이터 표가 2026-07-24 그대로 굳어 있었다** — ④작가가
   「`generate.ts` **스텁**」, 산문가·검열관이 「없음」. 실제 `generate.ts` 는 732줄이고
   같은 파일 §팔레트 절과 `NEXT-ACTION` 은 ✅ 로 적었다. **이 표가 「어디까지 왔나」의
   정본으로 읽히는 자리라 어긋남이 비쌌다.** 그 아래 「잡았어야 할 역할」 표의 ❌ 셋 중
   하나(유서)도 §9-7(c)로 이미 닫혀 있었다
2. **§캠페인 자동 생성의 「daily 고정(3인·2장·6공란)」 — 세 숫자가 전부 틀렸다.**
   코드는 `SUSPECTS = 5` 고정, 장 기본 5 · 범위 2~8
3. **`generate.ts` 머리말이 자기 파일 316행과 어긋났다** — 「장 수는 daily 고정 2장·7공란.
   campaign 규모면 검증 실패율 급등」. **게이트가 그 예측을 반증했다**: `gen-check` 는
   `--chapters` 를 안 주므로 **기본값 5장(=캠페인 규모)**으로 돌고 `--min-pass 100` 이다
4. **`NEXT-ACTION` 안의 자기모순** — 480행 「★ 언제나 5명(사용자 결정)」 vs 564행
   「5명 고정은 게임 규격이 아니다」. 코드가 5 고정이라 564행을 폐기 표시

### 묶음 2 — 장 수 6~8이 프로젝트 자체 기준을 깨고 있었다

사용자가 「8은 말이 안 된다」고 했고, **직관은 맞았는데 망가진 8은 옆 슬라이더였다.**

```
조사 대상이 34로 고정인데 예산만 장 따라 9→14로 늘었다
3장 3.8배 · 4장 3.4배 · 5장 3.1배 · 6장 2.8배 ❌ · 7장 2.6배 ❌ · 8장 2.4배 ❌
```

3배 미만이면 「선택이 소거가 된다」 경고. **슬라이더 범위 3~8 중 절반이 깨져 있었다.**

원인: 빈손은 남은 `(동사:대상)` 조합으로 만드는데 **가닥이 늘면 키를 뺏어가** 빈손이
정확히 그만큼 준다(3장 22빈손 · 8장 17빈손, 합은 둘 다 34). **제로섬이었다.**

고친 방법 — 조합을 늘렸다. 다섯 동사는 이미 포화였고(인물 전원·장소 전원·시신 하나)
**남은 것은 알리바이 쌍뿐**이었다:

- **알리바이 10쌍 전부 선언** (9개 신규 빈손). **앱은 이미 임의 쌍을 실행하고 있었다** —
  관계도에서 둘을 고르면 돌고, 선언 안 된 쌍은 `resultFor(...) || {type:'empty'}` 로
  공통 폴백에 떨어진다. **§9-8 의 거울상 — 앱이 주는데 사건 파일이 몰랐다**
- **피해자 조사 2종**(소지품·통화). 골든 케이스엔 `a_victim_bel` 이 있고 앱도
  `VICTIM_TARGET` 을 이미 배선해뒀다. 생성기만 자리를 안 만들었다
- `a_alibi` 라벨에 짝 이름 — 이것만 밋밋한 「알리바이 대조」면 그게 유용도 표시다
- **`EMPTY_SPOTS = 14` 삭제** — 선언만 되고 아무도 안 읽었다. 근거 주석이 「예산 7~8」
  이었는데 장 2 고정 시절 숫자다

**결과: 34 → 45. 장 3·5·6·7·8 × 난이도 3갈래 = 15조합 전부 통과율 100% · 소거 경고 0.**

### 묶음 3 — 팔레트 복붙 왕복이 사라졌다

**레지던시·박물관·방송국 팔레트가 저장소에 커밋돼 있는데 앱은 매번 붙여넣으라고 했다.**
이 저장소가 반복해서 잡아온 「이미 있는데 배선만 없다」 형태.

- **2번 절이 「세계를 고른다」로** — 내장 4종(레지던시·박물관·방송국·기본 어휘) +
  직접 만들기. 앞의 넷은 **복붙 0회**. `?raw` 로 읽어 붙여넣기와 **한 코드로** 지나간다
- **고른 세계·붙여넣은 팔레트를 저장한다**(`nobody-lies:palette`) — 라우트가 바뀌면
  `key` 로 재마운트되므로 전에는 매번 빈칸이었다
- **사건 개수 슬라이더 제거 → 한 번에 하나.** 「시도할」의 근거(검증 탈락)가 통과율
  100% 로 죽었다. **라벨을 트릭 개수로 읽는 오독이 두 번 났고**(07-29 주석 + 이번 사용자),
  두 번째에 손잡이를 없앴다 — **설명을 두 문단 붙여야 하는 손잡이는 손잡이가 틀린 것이다**
- 저장 키 `gen-48897-48897` → `gen-48897`

### 묶음 4 — 진술이 처음으로 읽힌다 · 프롤로그 · 피해자

**핵심 규칙(「무고한 사람은 거짓말하지 않는다, 다만 자기 비밀은 말하지 않는다」)이
생성 사건에서 작동할 자리가 없었다** — 다섯이 동선만 읊고 끝났다.

- **팔레트에 인물 층**: `secrets`(감출 것) · `openers`(말버릇). **왕복을 하나 더 만들지
  않은 이유**: 말투·비밀은 사건이 아니라 **세계의 속성**이라 팔레트에 들어가고,
  팔레트는 이미 내장이라 왕복 0회다 (`NEXT-ACTION` 3번이 적어둔 길)
- **전원 4문단으로 못박았다** — 비밀 가진 사람만 길어지면 §9-9 가 오류로 잡는 그것이다.
  범인도 똑같이 하나 받는다. 세 내장 팔레트에 그 세계 전용 6개씩 + `PALETTE-BRIEF` 갱신
- **엔진이 프롤로그를 만든다** — 없으면 `App.jsx:457` 이 갈아끼우지 않아 **산장 프롤로그가
  레지던시 사건에 떴다**(실측). §9-7(b) 때문에 가명·동기·기록 이름은 안 쓴다
- **피해자 표시줄 배선** — `'윤다인 (30) · 소설가'` 가 세 자리에 하드코딩이었다.
  `applyCase` 는 `victimProfile` 을 **조사 대상용으로만** 읽었다. 07-28 「제목만 안
  읽었다」와 같은 부류
- **2차 브리프가 프롤로그까지 받는다** — `PROSE-BRIEF.md` 는 원래 요구하는데 생성기가
  「아래 자리만 채우세요」로 잘라내고 `applyProse` 도 `people` 만 병합했다

### 검증기 — §9-10 신설, 그리고 등급을 내렸다

**진술이 조사로 얻을 단어를 말하는가.** §9-7 은 프롤로그·결과문·서사를 읽는데
**산문 중 가장 긴 진술만 안 읽었다.** 팔레트 `secrets` 가 진술로 직접 들어가므로 필요했다.

> ⚠️ **오류로 걸었다가 경고로 내렸다.** 만들자마자 골든 케이스가 걸렸는데 걸린 자리가
> 백리원의 *"요즘 연예계에 마약이 돌고있다는 소문이요"* — **결함이 아니라 그 인물이
> 산장에 온 이유**다. 그리고 **낱말을 들은 것과 확보한 것은 다르다**(공란 후보가
> `candidates: discovered` 라 은행만 본다). §9-7(c)(d)와 같은 등급으로 맞췄다.

### 검증한 것

| | |
|---|---|
| 조사 3배 | 장 3·5·6·7·8 × easy/normal/hard **15조합 전부** 100% · 소거 경고 0 |
| 내장 세계 | 레지던시·박물관 **복붙 0회 생성** · 장소 11곳 전부 그 세계 · 박물관 확보 단어에 야간 순찰 일지·반출 신청서·보안 카드 기록 |
| 팔레트 저장 | 홈 → 다시 생성기 = **박물관 유지** |
| 조립 진술 | 5명 전원 4문단 · 말버릇 5종 다름 · 비밀 5종 다름 |
| **2차 브리프 왕복** | YAML 붙여넣기 → **「넣었다 — 5명 4문단씩」** · 행이 「산문 입힘」으로 |
| **반려 경로** | 9문단 대 1문단 → §9-9 가 물고 **원본 안 망가짐**(앞서 넣은 4문단 유지) |
| 프롤로그 | 레지던시 사건에 레지던시 프롤로그 (전엔 산장 것) |
| 피해자 | **네 조합 실측** — 산장 ko 윤다인/en `Kim Chae-won` **보존** · 생성 ko 조은결/en 한국어 폴백 |
| 콘솔 | 에러 0 · 사용자 `nobody-lies:mountain-lodge` 진행 저장 무사 |

## In Progress

없음.

## Remaining Tasks

- [ ] **🎯 ① 무고한 넷이 서로 구분되지 않는다 (다음 하나 — 사용자가 지정)**
      넷의 동선 문장이 **글자까지 같다.** 오늘 넣은 말버릇·비밀만 다르고 알맹이는 하나다.
      원인은 `generate.ts` `innocentPresence` **한 배열을 넷이 공유**하는 것 —
      검증기가 「무고한 넷은 사망 구간에 현장 금지」를 강제하니 가장 쉬운 해가
      「전원 t2 홀」이었다. **진술 정독이 실질 2명분**(범인 1 + 무고 1)이고 격자도 같은 줄.
      **장소가 11곳이라 t1 현장만 피하면** 서로 다른 (시간대, 장소)를 줘도 배제가 유지된다
- [ ] **② 장을 완성해도 아무 일도 안 일어난다** — `reveals` 가 **1장짜리 하나로 하드코딩**
      (`generate.ts` 977행). 산장은 6개(서사 6 · 주장 추가 4). 5장 사건인데 **2~5장 완성이
      전부 무음**이다. 서사 문안이 필요해 2차 브리프를 또 넓혀야 한다
- [ ] **③ 지문(gesture) 0** — 산장은 5명 전원. §9-1 이 전원/전무를 강제하니 0도 합법이지만
      진술 화면이 밋밋하다. 2차 브리프가 「지문은 넣지 마세요」로 스스로 막아둔 상태
- [ ] **트릭 개수를 난이도 손잡이로** — 사용자가 뒤로 미뤘다. `schema.ts:441` 이
      `trick.types` 를 **배열로** 받고 검증기가 순회하는데 **생성기만 하나씩 넣는다**
      (`types: ['locked_room']` … · 396행이 아키타입 하나만 뽑음). `NEXT-ACTION:498` 이
      이미 결정해둔 것. ⚠ `delayed_mechanism` 은 격자가 달라 조합에서 빼야 할 수 있다
- [ ] **`Palette.spots` 가 죽었다** — 타입 선언·기본값이 있고 `palette-residency.json` 이
      실제로 주는데 읽는 곳이 없고 `PALETTE-BRIEF.md` 는 요구조차 안 한다. **안 지웠다** —
      빈손 이름을 팔레트 어휘로 짓는 자리가 될 수 있고 그건 저작 결정이다
- [ ] `e_safes` · `a_ph_yuri` 의 `clues` — 사용자 결정 대기 (07-27부터 상주)
- [ ] 색·대비·질감 · 배치 스냅 · 표기 안내 문안

## Blockers / Issues

- ⚠️ **게이트가 JSX 를 안 본다.** `seedBase is not defined` 를 7단이 전부 초록으로
  통과시켰다 — 저장 키가 쓰던 변수를 지웠는데 타입체크는 `engine/` 만 돈다.
  **브라우저에서 눌러서 잡았다.** 문서의 *"빌드 통과는 이식 여부를 증명하지 않는다"* 그대로
- ⚠️ **07-28 결함을 그대로 다시 밟을 뻔했다.** 피해자 영문을 `vp.en ?? line` 으로 썼는데
  그러면 산장의 `Kim Chae-won (30) · Novelist` 가 한국어로 뭉개진다 — **제목에서 이미
  당한 자리다.** 제목이 쓰던 세 갈래(`_foreignCase`)로 고쳤다. **엔진→앱 이관은 항상
  영문을 먼저 확인한다**
- ⚠️ **`read_page` 가 0x0 을 준다** (창이 표시 상태가 아니면 compositing 없음).
  스크린샷·클릭 도구를 못 쓴다. **`javascript_tool` 로 DOM·localStorage·React fiber 를
  직접 읽어** 전부 확인했다 — 이 우회로가 이번에도 통했다
- ⚠️ **PowerShell 이 standalone `--` 를 삼킨다.** `npm run verify -w engine -- --generate`
  가 인자 없이 돌아 **골든 케이스를 검증하고 초록을 냈다.** 하마터면 「3장도 hard 된다」를
  틀린 근거로 말할 뻔했다. **엔진 CLI 는 Bash 로 돌린다**
- ⚠️ `--palette` 경로는 **engine/ 기준**이다 — `templates/palette-residency.json`

## Notes for Next Session

- **`before-work` 순서 그대로.** `git pull` 이 맨 앞. 다른 기계로 넘어가므로
  **`npm install` 이 필요할 수 있다**(워크로그 frontmatter 의 `machine` 과 비교).
  lock 에 `"peer": true` 만 지운 차분이 생기면 **그 자리에서 되돌린다**
- **게이트는 7단이다** — `package.json` 의 `build` 를 믿는다(문서 숫자가 아니라)
- **생성기 흐름이 바뀌었다**: 1 크기 → **2 세계 고르기(내장 4종)** → 3 사건 만들기 →
  4 목록 → 5 산문 입히기. **팔레트 복붙은 「직접 만든다」를 골랐을 때만** 나온다
- **왕복은 이제 사건당 최대 1회이고 그것도 선택이다** — 조립 진술로 바로 플레이할 수
  있고, 더 좋게 쓰고 싶을 때만 5번 절로 덮어쓴다
- **`localStorage.clear()` 금지** — dev 서버와 같은 origin이라 사용자 진행이 날아간다.
  이번 검증도 `gen-` 접두 키만 골라 지웠다
- 이 저장소는 **직접 main 푸시** (feature 브랜치·PR 없음)

## Files Modified

- `engine/src/generate.ts` — 알리바이 10쌍 · 피해자 조사 2종 · `EMPTY_SPOTS` 삭제 ·
  `Palette.secrets`·`openers` · `statementOf` 재작성 · **프롤로그 생성** · 머리말 정정
- `engine/src/verifier.ts` — **§9-10 신설** (진술이 조사 단어를 말하는가 · 경고)
- `engine/templates/PALETTE-BRIEF.md` — `secrets`·`openers` 요구 + 항목별 주의
- `engine/templates/palette-{residency,museum,example}.json` — 세계별 `secrets`·`openers` 6개씩
- `app/src/Generator.jsx` — **내장 세계 4종** · 팔레트 저장 · 사건 개수 슬라이더 제거 ·
  저장 키 정리 · **2차 브리프에 프롤로그** · 문구 정정(「자리표시」→「조립 진술」)
- `app/src/App.jsx` — **피해자 표시줄 배선**(`VICTIM_LINE`·`victimLine()`) · 세 자리 연결
- `docs/MEMORY.md` — §오케스트레이터 표 갱신 · §선행 조건 닫음 · §캠페인 숫자 정정
- `docs/NEXT-ACTION.md` — 용의자 수 자기모순 폐기 · 날짜 함정 배너
