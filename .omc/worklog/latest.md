---
session_date: "2026-08-02"
project: "노바디 라이즈 (Nobody Lies)"
working_directory: "C:/Users/kkh94/OneDrive/Desktop/Nobody Lies"
machine: "KWONKYUNGHUN"
---

## Completed Work

**게이트 12단 초록(exit 0) · 커밋 넷 · 푸시 완료.**
게이트가 **11단 → 12단**이 됐다(`clue-check` 7단째 — `prop-check` 과 `tmpl-check` 사이).

> ★★ **오늘 가장 큰 것 — 뒤집기가 섰다** ★★
>
> `MEMORY.md` §근본 진단이 08-01에 세운 *"생성이 거꾸로 돈다"* 를 닫았다.
> **재고 나서 같은 세션에 지었다.**
> ```
> 전   buildWorld → buildGameLayer(답을 첨자로) → 서술문 → verifier 가 사후 검사
> 후   buildWorld → buildGameLayer → closeClues   ← 증명이 생성 경로 안으로
> ```

### ① 먼저 쟀다 (`3351eed`) — 그리고 **질문의 전제가 무너졌다**

```
                generate.ts            검사 쪽          구조
인물 48개   실행 13줄 · 주석 38    verifier.ts +86   buildGameLayer 안 · 시그니처 무변경
시각 60개   실행 18줄 · 주석 29    proof.ts    +60   buildGameLayer 안 · 시그니처 무변경
```

- ★ **비싼 쪽이 반대였다** — 단서 까기 20줄 미만, **검사 짓기 60~86줄.** 문서의
  「대략 30줄씩」은 **주석 포함** 값이었다. 그러면 뒤집기의 값은 단서 까기가 아니라
  **사후 검사를 매번 새로 안 지어도 되게 만드는 것**이다.
- ★★ **밀린 공란이 없다** ★★ *"나머지 공란은 이미 단서가 있나"* 의 답이 **전부 있다**
  (`proof-check 20` → **320/320** · 라벨 아홉 전부 100%). **값을 매길 자리가 없었다.**
- **구조를 안 흔든다** — `generateCase` 본문이 **한 줄**이라 루프 자리가 이미 비어 있었고
  `buildWorld` 1,343줄 무변경. 이동 메뉴 = 발화 규칙 여섯 중 **다섯이 국소**
  (R2 유죄 요건만 세계 층인데 **승리 조건 그 자체라 수리가 필요할 일이 없다**).
- **예산 결합을 의심했는데 안 걸렸다** — `orchestrate.ts:97` 이 완성된 사건에서 예산을
  도출한다. 순서가 `build → 루프 → probe → budget`.

### ② 그리고 지었다 (`de037ad`)

| | |
|---|---|
| `engine/src/clues.ts` | `weakBlanks`(종료 조건) · 이동 둘 · `closeClues`(루프) |
| `engine/src/clue-check.ts` | 게이트 12단째. **두 단이다** |

★ **종료 조건이 이 작업의 값이다:**
```
unique  ∧  ( cost > 0  ∨  걸음의 규칙이 선언된 전제 )
```
없으면 루프가 **무료 관측에 단서를 박는 쪽으로 수렴**해 `proof-check` 100% 인데
**게임은 조사가 필요 없어진다.** ⚠ **blanket `cost > 0` 도 안 된다** — 비용 0 서른이
**정확히 R7(20)+R5(10)**, 곧 **선언된 전제 둘**(발견 시각·현장)이라 통째로 걸린다.

**이동은 `proof.ts` 규칙의 역방향이다** — M1 발견 시각 전제(R7↔) · M2 기록이 가리킨다(R6↔).
★ **M1 에만 제약을 걸었다** — 브리핑은 **무료 관측**이라 아무 시각이나 적으면 그게 곧
누설이고 **이 루프가 저지르기 가장 쉬운 배신이 정확히 그것**이다. 다시 모인 칸(발견
시각)만 예외인 이유는 사건이 시신이 발견되면서 열리기 때문이다.

**증명 (주장 아님)**
```
gen-baseline   회귀 0 — 64건이 기준선과 완전히 같다   ← 루프가 출력을 한 글자도 안 바꿨다
clue-check ①   생성 40건 · 공란 640 · 약한 공란 0
clue-check ②   M1 심어서 1개 물림 → 닫힘 · M2 심어서 2개 물림 → 닫힘
게이트          12단 exit 0
```

★★ **②가 없으면 이 검사는 거짓말이다** ★★ 착수 시점에 이미 320/320 이라 **①은 이동이
전부 죽어 있어도 초록**이다. 이 저장소는 그 부류로 여러 번 물렸다(`TRICKS` 죽은 인자 ·
`COLLECTED_POOL` decoy 3 · `REVEALS[].yield`). **심은 것이 안 물려도 실패하게** 해뒀다.

### ③ 낡은 것을 따라 고쳤다 (`96120a0`)

- `MANIFESTO §남은 일` ①이 여전히 *"고칠 것은 루프다"* 라고만 말했다 — **최우선 문서라
  매 세션 0번으로 읽힌다.** ✅ 를 달고 종료 조건·회귀 0 을 적었다.
- `NEXT-ACTION` 머리말이 **「커밋 여덟 · 게이트 10단」**인데 본문은 **11단**이었다.
  `git log` 로 세니 08-01 커밋이 **22개(8+14)** — 그날 세션이 둘이었고 뒤 세션이 본문만
  갱신했다.

### 오픈소스 (MANIFESTO §2·§3)

**이번 세션에 의존성을 안 늘렸다.** `clues.ts` 는 `proof.ts` 만 쓰고 `proof.ts` 는
`types.js` 만 import 한다 — **순환 없음.** 08-01의 `logic-puzzle-generator`(MIT) 개념
차용이 이미 `proof.ts` 에 들어와 있고 그 위에 얹었다.

## In Progress

없다. 게이트 초록 · 트리 깨끗 · 푸시 완료에서 멈췄다.

## Remaining Tasks

- [ ] 🎯 **테스터에게 URL 을 보낸다** — **사용자 손. 다섯 세션째.**
      `https://nobody-lies-app.vercel.app/#case=practice-room` (첫 판 권장 · 3장 · 20~30분)
      ⚠ 커밋된 4건은 08-01 페어플레이·다양성 작업 **이전에 뽑힌 것**이라 새 것을 보이려면
      사건을 다시 뽑아야 한다. ⚠ `closing-theater` 는 목록에서 뺀다(프롤로그 불일치 의도).
      ★ **코드 쪽만 계속 밀면 이건 안 닫힌다.**
- [ ] 🎯 **다음 하나 — `verifier §5` 의 틀린 명제를 갈아 끼운다**
      §5 가 보는 것은 **장 전체의 조합 수 ≥ 30**(찍기 난이도)이라 **공란 하나가 근거 없이
      정해져도 곱이 크면 통과한다** — 08-01에 인물 48개가 그 구멍으로 빠졌다.
      *"정확한데 틀린 명제는 정밀도로 못 고친다."*
      ```
      지금    §5  장의 blanks 조합 수 ≥ 30
      바꾸면  §5  공란마다 도출 가능한가 (proveBlanks 가 잰다)
      ```
      ★ **08-02 이전엔 대안이 없었는데 지금은 손에 있다** — `clue-check` 이 생성 쪽을
      이미 물고 있어서, **검사 둘이 서로 다른 명제를 말하는 상태**로 두면 안 된다.
      ⚠ **경고로 시작한다** — `proof-check` 이 커밋 4건에 **35/61(57%) · 비용 0 셋**이라고
      인쇄한다. 손저작은 산문을 나중에 입히므로 오류로 걸면 **검사가 거짓말이 된다**(§6.8 선례).
- [ ] ⚠️ **§9-14 가 전건에서 운다 — 물증 카드가 비어 있다** (생성 40/40 · 산장 3 · 연습실 2 ·
      **저작 서식 3**) 확보 단어를 주는 물증인데 `record` 가 빈다. **산문 왕복을 거친
      둘만 0** 이라 **산문가가 메우고 있는 구멍**이다.
      ★ **저작 서식(`case-template.yaml`)도 같은 id 셋(`e_alias`·`e_alias2`·`e_motive`)에서
      운다** — 07-30 §9-3h 때처럼 **사람이 베끼는 파일이 같은 구멍을 가르치고 있다.**
      생성기만 고치면 서식이 그대로 남는다.
- [ ] 🎯 **행렬을 채운다 — 빈칸 19 + 부분 8** (`npm run matrix`)
      가장 큰 셋이 전부 산문 층: **진술 × 모순** · **추가 진술 × 모순** · **보고서 × 모순**
      ⚠ **어휘 봉쇄를 순진하게 만들면 안 된다 — 20/20 거짓 양성이다**(거리 참조점 ·
      합성명사). 필요한 것은 「낱말이 있나」가 아니라 **「위치 주장인가」**이고 왕복 계약을
      바꿔야 한다.
- [ ] **§번호 → 행렬 좌표 개명** (외부 조언에서 건진 것) — `§6.55`·`§6.56`·`§6.57` 이
      **언제 붙었나**를 말하지 **무엇을 검사하나**를 안 말한다(`MEMORY §근본 진단` ①).
      개명하면 `matrix.ts` 가 **사후 발명이 아니라 명세**가 된다. 싸고, 이후 작업의 좌표계다.
- [ ] **R1 소지품 고리의 역방향 이동** — 08-02에 **일부러 안 지었다.** 서술문 배열 수술이
      필요한데 공란이 이미 참조돼 있어 **문장이 깨질 수 있다.** 지금은 못 깔면
      `clue-check` 이 **exit 1 로 크게 실패**한다 — 조용히 통과보다 낫다.
- [ ] `es-hangul` 이관 — 조사 규칙이 **다섯 벌**(엔진 넷 · 앱 하나)
- [ ] 별건 둘 — DS 번들 `createRoot` 충돌 · 사이드바 밖 클릭 요소 137곳

## Blockers / Issues

- ⚠️ **「홀로 옮겼습니다」가 「혼자서」로도 읽힌다** — 문법은 맞아졌는데 뜻이 겹친다. 문안 판단.
- ⚠️ **DS 번들이 `#root` 에 자기 앱을 마운트한다** — 매 로드 콘솔 4건. 별건.
- ⚠️ **사이드바 밖 클릭 요소 137곳**(span 89 · div 46) — 일괄 치환하면 안 된다. 별건.
- ⚠️ `closing-theater` 는 **프롤로그 불일치를 일부러 남긴** 사건 — 테스터 목록에서 뺀다.
- ⚠️ **대시보드 Override 를 다시 켜지 마라** — `vercel.json` 이 정본이다.
- ⚠️ **손저작 비용 0 셋을 아직 안 갈랐다** — 「전제라서 0」과 「누설이라서 0」을 지금은
  구분 못 한다(손저작은 규칙이 안 닿는 자리가 많아 57%). `verifier §5` 손볼 때 같이 본다.

## Notes for Next Session

- ★★ **워크로그가 최신이라는 보장이 없다** ★★ 오늘 손저작 증명률을 직전 워크로그에서
  `31/61(51%)` 로 **안 재고 인용**했는데 재보니 **35/61(57%)** 이었다. 그건 같은 세션
  **후반의 수리 이전** 값이었고, **`MANIFESTO` 쪽이 처음부터 맞았다.**
  **숫자는 인용하지 말고 돌려본다** — `proof-check` 은 5초다.
- ★ **오늘 부류가 하나였다 — 「내가 방금 지적한 것을 내가 저지른다」** ★ 세션을 열며
  `b9f49b4`(낡은 머리말)를 지적해놓고 같은 세션에 `MANIFESTO` 를 낡은 채로 둘 뻔했다.
  **최우선 문서는 매 세션 0번으로 읽히므로 여기가 낡으면 제일 비싸다.**
- **외부 조언 쓰는 법이 생겼다**(`MEMORY §외부 조언을 쓰는 법`) — **위험은 취하고 처방은
  버린다.** 오늘 챗봇 주장 셋 중 둘이 `proof.ts` 를 15줄 읽으니 갈렸다.
  ★ **발췌만 받은 조언은 「없다」를 말할 자격이 없다.**
- ⛳ **뒤집기가 하는 일을 헷갈리지 마라** — **「손으로 참인 것을 기계가 강제하게」** 하는
  것이지 **새 축을 덮는 것이 아니다.** 덮이는 범위는 `proof.ts` 의 `RULES` 가 전부고
  넓히는 것은 `matrix.ts` **빈칸 19** 쪽이다. **「100% 니까 끝났다」로 읽으면 안 된다.**
- ⛔ **`generateCase` 에서 throw 하지 마라** — 브라우저의 `Generator.jsx` 가 부르므로
  던지면 **유저 화면이 통째로 빈다**(2026-07-26 `StatusIcon` 선례). 무는 것은 게이트다.
- **구조 제안 다섯 중 ⑤가 닫혔다** — ①어휘 봉쇄 → ②대칭 축 → ③공란별 solver(08-01) →
  ④관측면 → **⑤생성 루프 되먹임 ✅(08-02)**. ★ **④는 선행 조건이 아니었다**(재서 확인).
- **재미·산문 두께 등 주관적 축은 기준에서 뺐다**(사용자 결정). 남은 축은 둘 —
  **범인/용의자가 구분되는가** · **트릭·물증이 완전히 구현되는가.**

## 참고 — 외부 공유용 정리

`docs/STATUS-2026-08-01.md` — 배경부터 담은 현재 완성도 정리. ⚠ **게이트 11단으로 적혀
있어 한 단 낡았다**(지금 12단 · `clue-check`). 다음에 갱신하거나 날짜를 올린다.

## Files Modified

```
engine/src/clues.ts             ★신규★ 되먹임 루프 (종료 조건 · 이동 둘 · closeClues)
engine/src/clue-check.ts        ★신규★ 게이트 12단째 (① 약한 공란 0 · ② 심어서 확인)
engine/src/generate.ts          generateCase 가 closeClues 를 돈다 (본문 1줄 → 3줄)
package.json                    build 에 clue-check · 루트 clue-check 스크립트
engine/package.json             clue-check 스크립트
docs/MANIFESTO.md               §남은 일 ① — 루프가 섰다 · RULES 범위 경계
docs/MEMORY.md                  §근본 진단에 §값을 쟀다 · §뒤집기가 섰다 · 08-02 절
docs/NEXT-ACTION.md             머리말(08-01 낡은 줄 정정 포함) · §다음 즉시 액션 · 게이트 12단
docs/SESSION-LOG.md             08-02 절(최신이 위) · 08-01 헤딩의 「10단」 정정
.claude/commands/before-work.md 게이트 11단 → 12단 · clue-check 두 단 설명
.omc/worklog/latest.md          이 파일
```
