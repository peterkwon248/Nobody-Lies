# DC 툴 동기화 changeset (2026-07-25)

> # ⛔ 이 파일은 닫혔다 (2026-07-27, 사용자 결정)
>
> **`app/src/App.jsx` 가 정본이 됐다. DC export 는 동결됐다.**
> 재export 를 하지 않으므로 **아래 1~12번은 DC 마스터에 반영할 필요가 없다.**
>
> 남는 일 하나 — **9번(표기 안내가 없는 것을 설명하고 있는 것)은 문안 저작이라
> 앱에서 해야 한다.** 나머지는 이미 앱에 적용돼 있다.
>
> 이 파일은 **기록으로 남긴다.** 어떤 것이 왜 어긋났는지가 다음 사건을 만들 때
> 같은 함정을 피하는 자료다. 특히 5번(인물명)·6번(조사)·10번(중복 키)은
> **기계 변환이 무엇을 보존하고 무엇을 못 보존하는지**의 사례다.
>
> `app/public/_ds/` 번들은 예외다 — 그건 여전히 export 산물이고, 자기 마운트와
> `Panels.jsx` 컴파일 실패가 그 안에 있다. `main.jsx` 의 방어를 지운다면 그때 다시 본다.

---

> ## 🔴 2026-07-26 — 앱이 DC React export 로 교체됐다
>
> 손으로 옮기는 이식을 접고 **DC 가 변환해준 React 를 앱으로 삼았다**
> (`app/src/App.jsx`). 그 export 는 **옛 DC 마스터에서 뽑혀서** 아래 1~7번이
> 전부 되살아나 있었고, 그 자리에서 다시 적용했다:
>
> | | 적용 결과 |
> |---|---|
> | 5. 인물명 | 치환 전후 1:1 확인 — 세라 16=쿠라 16 · 지안 12=예나 12 · 유빈 15=유진 15 · 나경 10=유리 10 · 리원 10=원영 10 · 다인 36=채원 36. 옛 이름 잔존 0 |
> | 6. 조사 | **8건**이었다(changeset 은 지문 2건만 적었다). 받침을 얻은 둘만 해당 — 지안(는→은·가→이·와→과 3) · 나경(는→은 2·와→과) |
> | 7. 이니셜 | 지·유·세·나·리. 두 표(`PEOPLE`·`PB_CARDS`) 다 |
> | 1~4 | 공범 라벨 4곳 · 4장 문장틀 · 별채 대포폰 서술 · 발견 위치 |
> | 예산 | **5 → 6.** 흩어진 하드코딩 8곳을 `BUDGET` 상수 하나로 모았다 |
>
> **8번은 이 export 에서 해결됐다 — 고칠 것이 없다.** 원본 `.dc.html` 은 `.app` 이
> 986행에서 닫히고 홈·상세·모달이 **형제**라서 토큰이 죽었는데, React 변환은 전부
> 하나의 트리 안에 넣는다. 실측 — 홈(z=70)·사건 상세(z=72)·인물 모달(z=90) 셋 다
> `.closest('[data-surface="vector"]')` 가 잡히고 `--g-lock-mark` 가 `#4CB782` 로
> 산다. **`:root` 로 올리지 않는다** — 지금 올리면 export 와 갈라지고 다음
> 재export 가 조용히 되돌린다. 그게 이 파일이 존재하는 이유 그 자체다.
>
> **9번(표기 안내)은 아직이다.** 문안 저작이라 별도.
>
> **이 파일은 여전히 살아 있다.** DC 마스터는 그대로 옛 상태이고, 다음
> export 를 받으면 위가 **또** 되살아난다. 근본 해결은 DC 툴에 반영하는 것뿐이다.
>
> export 자체의 결함도 하나 고쳤다 — `relChip()` 이 CSS **문자열**을 돌려주는데
> `style={st.relStyle}` 로 꽂혀서 **진술 화면을 열면 앱이 통째로 언마운트됐다.**
> `S()` 로 감쌌다. DC 변환이 이 하나만 빠뜨린 것이라 재export 때 다시 볼 것.
>
> **2026-07-26 (2차) — export 결함 둘 더.** 재export 때 다시 볼 것:
>
> | | |
> |---|---|
> | `_ds_bundle.js` **끝(2283행)에 자기 앱 마운트가 있다** | `ReactDOM.createRoot(document.getElementById("root")).render(<App/>)`. `standalone.html` 은 그 한 줄로 도는 물건이라 그렇다. 우리도 같은 `#root` 를 쓰므로 **두 앱이 겹쳐 렌더되고 사이드바 클릭이 죽은 트리로 간다.** 우리 앱은 나중에 그려서 순서 운으로 이기고 있었고 단서는 콘솔 경고뿐이었다. `main.jsx` 가 번들 로드 중에만 `window.ReactDOM.createRoot` 를 빈 루트로 바꿔 삼킨다 — **번들을 고치지 않았으므로 재export 에 그대로 온다(우리 쪽 방어는 남는다)** |
> | `buildProfiles()` 에 **중복 키** | `{ … color: p.color, ini: p.ini, color: p.color, … }`. 같은 값이라 무해하지만 `vite:esbuild` 가 매 빌드에 경고한다. 저장소에서는 뒤의 것을 지웠다 — **DC 마스터에서 지워야 안 돌아온다** (아래 10번) |
>
> **미해결 — 번들 내부 순서**: `ds.__errors` 에 한 건이 남아 있다.
> `{"path":"ui_kits/app/Panels.jsx","error":"StatusIcon is not defined"}` —
> `ds.StatusIcon` 은 함수로 존재하는데 `Panels.jsx` 컴파일 시점엔 없다. 매 로드마다
> 찍히고 있다. **무엇이 폴백되는지 아직 확인하지 않았다.**

> **왜 이 파일이 있나.** 게임 로직의 정본은 `engine/`이고 프로토타입은 그 미러다.
> 미러가 어긋난 항목을 여기 모아둔다. 적용 대상은 **DC 저작 툴의** `추리게임.dc.html`
> (`dc_js_str_replace`). 저장소 파일은 DC 워크스페이스의 사본이다.

## ⚠️ 아래 5건은 **저장소 쪽에는 이미 적용됐다. DC 툴에는 아직이다** (2026-07-25)

플레이테스트를 더 못 미루는 상황이라 저장소 사본과 **배포본에 직접 적용했다**:

```
prototype/추리게임.dc.html
prototype/추리판.dc.html                          (인물명만 해당)
prototype/playtest/Nobody Lies (노바디 라이즈).html   ← 실제 배포본
```

적용 후 배포본을 브라우저로 열어 렌더·순차 잠금·콘솔 에러 0 확인. 옛 이름 잔존 0건,
치환 전후 출현 수 1:1 일치(세라 16 = 쿠라 16, 다인 36 = 채원 36 …).

> **그래서 이 파일은 아직 살아 있다.** DC 워크스페이스(마스터)는 여전히 옛 상태이므로
> **거기서 재export 하는 순간 위 수정이 전부 사라진다.** 다음에 DC 툴을 열면
> 아래를 먼저 반영하고 export 할 것.

---

> **2026-07-25 재작성 이유.** 두 기계가 병렬로 작업하면서 이 파일이 실제 상태와
> 어긋났다. 병합 시 새 DC export(2946줄, 평면도 GEO 엔진 + 상황판 인라인)를 정본으로
> 채택하고 그 export 를 한 줄씩 확인해 **이미 적용된 것과 남은 것을 갈랐다.**
> 아래는 확인된 것만 적는다.

---

## ✅ 이미 적용됨 — 다시 하지 말 것

DC 툴 쪽에서 이미 반영돼 새 export 에 들어와 있다. 엔진과 일치하는 것을 확인했다.

| 항목 | 확인 근거 |
|---|---|
| **4장 협박대상 = 조유리** | `b15: { kind: 'vTarget', src: 'person', ans: '조유리' }` |
| **확보 단어 획득 그래프** | `TERM_MAP` 의 `search:annex` = `[별채 대포폰, 김선생, 폭로 임박]` · `belongings:wonyoung` = `[김선생, 마약, 폭로 임박]` · `belongings:yuri` = `[마약, 치정]` — 엔진 `e_burner`·`e_wy_text`·`e_yuri_threat` 와 각각 완전 일치 |
| **`폭로 임박` 무료 공개 제거** | `REVEALS.s4` 에 `terms` 키가 없다 (유리 진술 statement 만 남음) |
| **`대포폰` 동의어 후보 제거** | `COLLECTED_POOL` 에 `대포폰` 없음. `별채 대포폰` 하나만 후보로 뜬다 |

남아 있는 `'대포폰'` 4건은 전부 **표시 라벨**이다(아이콘 사전 · 물증 카드 label ·
관계 그래프 노드 · 평면도 마커). 후보 목록이 아니라 자연어 지칭이라 오답을 유발하지
않는다 — 그대로 둔다.

---

## 1. `공범` 라벨(`vAccomplice`) 제거 — 4곳

라벨 존재 자체가 "범인 말고 한 명 더 있다"를 **조사 0회에** 누설한다.
진실 세계의 `accomplice` 역할은 유지하고 공란 어휘에서만 뺀다.
엔진은 `BlankLabel`·`DOMAIN_OF` 양쪽에서 이미 제거됐다.

```
ko 라벨 사전   ... vStaging: '위장물', vAccomplice: '공범', vLastSeen: ...
             → ... vStaging: '위장물', vLastSeen: ...

en 라벨 사전   ... vStaging: 'Staging', vAccomplice: 'Accomplice', vLastSeen: ...
             → ... vStaging: 'Staging', vLastSeen: ...

CAT           ... vTarget: 'susp', vAccomplice: 'susp' }
             → ... vTarget: 'susp' }

catL          ... vTarget: '협박대상', vAccomplice: '공범' }
             → ... vTarget: '협박대상' }
```

---

## 2. 4장 서사 문장틀 — 답은 고쳤는데 문장이 안 따라왔다

`b15` 답은 `조유리`로 바뀌었는데 문장이 아직 "입막음"을 묻는다.
조유리는 입막음 대상이 아니라 **약물로 붙들린** 쪽이다. 답이 아니라 문장을 고친다.

```
- { text: '의 것이었다. 조직은 ' }, { b: 'b15' }, { text: '의 입을 막으려 했고, 며칠 전 ' }
+ { text: '의 것이었다. 조직은 ' }, { b: 'b15' }, { text: ' 약물로 붙들어 두고 있었고, 며칠 전 ' }
```

---

## 3. `별채 대포폰` 서술 — 존재하지 않는 두 번째 폰

`TERM_INFO` 의 서술이 "별채 안쪽에 놓여 있던 **또 다른** 선불폰"인데 사건에 폰은
하나뿐이다. 짧은 이름 `대포폰`을 후보에서 뺄 때 서술은 같이 안 고쳐졌다.
엔진 `e_burner.record` 로 맞춘다.

```
- '별채 대포폰': { fk: '별채 수색', dk: '별채 안쪽에 놓여 있던 또 다른 선불폰이었다.',
                  fe: 'Annex search', de: 'Another prepaid phone, set inside the annex.' }
+ '별채 대포폰': { fk: '별채 수색', dk: '가입자 정보가 없었고, 저장된 번호는 하나뿐이었다.',
                  fe: 'Annex search', de: 'No subscriber on record; only one number was saved.' }
```

---

## 4. 조사 결과 카드 — 발견 위치

엔진 `e_burner.foundAt = '별채 · 게임기 뒤'`.

```
- '별채 서랍에서 발신 전용 대포폰. ...'        'A burner phone in the annex drawer, ...'
+ '별채 게임기 뒤에서 발신 전용 대포폰. ...'   'A burner phone behind the annex game console, ...'
```

---

## 5. 인물명 — 엔진·문서는 새 이름, 프로토타입은 옛 이름

엔진 YAML 과 `docs/` 는 2026-07-24에 교체됐는데 프로토타입은 아직 옛 이름이다.
현재 export 기준 출현 수: `사쿠라` 13 · `김채원` 11 · `미야와키` 8 ·
`장원영` 5 · `조유리` 4 · `최예나` 3 · `안유진` 3.

| id | 옛 이름 | 새 이름 |
|---|---|---|
| `sakura` | 미야와키 사쿠라 (쿠라) | **문세라** (세라) |
| `yena` | 최예나 | **서지안** |
| `yujin` | 안유진 | **한유빈** |
| `yuri` | 조유리 | **오나경** |
| `wonyoung` | 장원영 | **백리원** |
| `chaewon` (피해자) | 김채원 | **윤다인** |

애칭·호칭(`쿠라언니`·`채원언니`)까지 같이 바꿔야 한다. `docs/design-brief.md` §진술
5개가 교체된 원문을 갖고 있으니 그것을 그대로 쓰면 된다.

> 이름 교체는 **상업 출시 전 필수**이면서 지금 하는 것이 싸다 — 진술 원문이 아직
> 프로토타입에 하드코딩돼 있어서, 산문을 사건 파일로 이관(A-2)한 뒤에 하면 두 곳을
> 고치게 된다.

---

## 6. `STMT_GESTURE` 조사 오류 2건 (2026-07-25 발견)

이름 교체(5번)를 하면서 **조사가 따라가지 않은 자리**가 지문에 둘 남아 있다.
`추리게임.dc.html` **1601·1604행**.

| 현재 (틀림) | 고칠 것 | 왜 |
|---|---|---|
| `지안는 팔짱을 낀 채…` | **`지안은`** | 옛 이름 `예나`(받침 없음 → `는`)의 잔재. `지안`은 받침이 있다 |
| `나경는 장바구니를…` | **`나경은`** | 옛 이름 `유리`(받침 없음 → `는`)의 잔재. `나경`도 받침이 있다 |

나머지 셋(`세라는`·`유빈은`·`리원은`)은 맞다.

> `b15` 의 조사(`par`)를 고칠 때 **문장을 바꾸면 조사가 따라가야 한다**고 적어뒀는데,
> 같은 규칙이 이름 교체에도 적용된다는 것을 그때 놓쳤다. 이름은 문장의 일부다.

**엔진 쪽은 이미 고쳤다** — 지문 10줄이 `mountain-lodge.yaml` 의
`people[].statement.gesture` 로 이관되면서 올바른 조사로 들어갔다. 이 항목은
DC 마스터에만 남은 것이다.

---

## 7. `PEOPLE[].ini` 아바타 이니셜이 옛 이름 잔재 (2026-07-25 발견)

`추리게임.dc.html` **1417~1421행**. 이름은 새것인데 이니셜이 옛것이다.

| 인물 | 현재 `ini` | 어디서 왔나 | 고칠 것 |
|---|---|---|---|
| 서지안 | `예` | 최**예**나 | **`지`** |
| 한유빈 | `유` | 안**유**진 | `유` (우연히 같다) |
| 문세라 | `쿠` | 사**쿠**라 | **`세`** |
| 오나경 | `리` | 조유**리** | **`나`** |
| 백리원 | `원` | 장**원**영 | **`리`** |

**넷이 틀렸다.** 아바타는 진술 화면·우측 패널·프로필에 전부 뜨므로 눈에 잘 띈다.

> 5번(인물명 교체)·6번(지문 조사)과 **같은 뿌리**다 — 이름을 바꿀 때 이름에서
> 파생된 것들이 따라가지 않았다. 조사·이니셜·애칭이 전부 그 부류다.

**앱은 규칙으로 계산한다** — `initialOf()`(`app/src/case/people.ts`)가 이름의 두 번째
글자를 쓴다(성 한 글자 + 이름 두 글자). 필드로 두면 또 갈라지기 때문이다.

---

## 8. `--g-*` 게임 토큰이 셸 밖에서 전부 죽는다 (2026-07-26 발견)

**`--g-*` 아홉이 `[data-surface="vector"]` 스코프에만 정의돼 있다**(31~33행).
그런데 셸(`.app`)은 986행에서 닫히고 **홈 · 사건 상세 · 모든 모달은 그 밖**에 산다.

```
73    <div class="app" data-surface="vector">   ← 셸. --g-* 가 여기 산다
986   </div>                                    ← 셸이 닫힌다
997   isIntro   ┐
1066  isHome    ├ position:fixed 오버레이. 셸 밖이다 → --g-* 가 없다
1112  isDetail  ┘
```

**값이 틀린 게 아니라 변수가 없다.** 그래서 색 선언이 통째로 무효가 되고
`transparent` / `currentColor` 로 떨어진다. 확인된 것 셋:

| 어디 | 무엇이 | 어떻게 보이나 |
|---|---|---|
| 홈·상세 `statusChip()` 2037행 | 「클리어」 칩 `--g-lock-mark`·`--g-lock-bg` | 글자색 상속 · 배경 투명 |
| 인물 상세 **1199행** | 단서 카드 `border-left: 2px solid var(--g-confirm)` | 흰 선 |
| 조사 결과 카드 **1231행** | `invResultCard.accent`(유형색) | 3px 띠가 투명 |

같은 칩의 「진행 중」(`--status-progress`)·「미플레이」(`--bg-elevated-2`)는 Vector
전역 토큰이라 멀쩡하다 — **한 줄 안에서 어떤 색은 살고 어떤 색은 죽는다.**

**고칠 것** — 33행 `[data-surface="vector"] {` 를 **`:root {`** 로. 값은 그대로 둔다.
참조 대상(`--fg-4`·`--accent`·`--status-review` …)이 전부 Vector 의 `:root` 토큰이라
스코프를 올려도 달라지는 것이 없다.

> 셸 안에서만 확인하면 절대 안 보인다. 홈의 클리어 칩은 다섯 장을 다 채워야
> 나오고, 나머지 둘은 모달이다. 앱에 최종 제출·채점과 인물 상세를 붙이면서 드러났다.

**앱은 이미 고쳐져 있다** — `app/src/styles/app.css` 의 `--g-*` 블록이 `:root` 에 있다.

---

## 9. 표기 안내가 **없는 것을 설명하고, 있는 것을 빠뜨린다** (2026-07-26 발견)

`buildRef()`(**2753~2811행**). 범례의 약속은 *"이 게임에서 쓰는 기호와 상태의
읽는 법입니다"* 인데 양쪽으로 어긋나 있다.

**없는 것을 설명한다** — 앱은 이 셋을 빼고 옮겼다:

| 항목 | 원본 | 왜 뺐나 |
|---|---|---|
| `sounds` 4항목 | 2804~2809 | 소리가 **하나도 없다** |
| 「도착 신호 · 안 읽음」 | 2794 | 사이드바 안 읽음 점이 없다 |
| 「보드 모드 · 심증판」 | 2796 | 공란 슬롯에 카드를 끄는 방식이 없다 |

**있는 것을 빠뜨린다** — 앱도 그대로 두었다(문안을 새로 쓰는 건 이식이 아니라 저작):

| 빠진 것 | 근거 |
|---|---|
| 공란 **잠김** 상태 | `lock` 스타일이 **2757행에 정의돼 있는데 `blanks` 배열이 안 쓴다.** 보고서엔 3상태가 다 있다 |
| 마킹 **「표시」** | 진술 하이라이트는 4색인데 범례는 확인·의심·모순 3색만 |
| 상황판 · 메모장 · 채점 화면 | `newStates` 14항목에 아예 없다 |

**고칠 것** — 위 표 둘. 없는 셋은 지우고, 빠진 셋은 문안을 새로 쓴다.

> 범례가 없는 기능을 설명하면 그게 곧 거짓말이다. 「모든 진술을 의심하라」가
> 태그라인인 게임에서 **범례만은 의심할 필요가 없어야 한다.**

**앱은 「없는 것」 셋만 뺀 상태다** — `app/src/screens/Reference.tsx` 머리 주석.

---

## 10. `buildProfiles()` 중복 키 `color` (2026-07-26 발견)

```js
return { id: p.id, name: p.name, color: p.color, ini: p.ini, color: p.color, … }
//                               ^^^^^^^^^^^^^^^            ^^^^^^^^^^^^^^^
```

같은 키·같은 값이 두 번. 동작은 같지만 `vite:esbuild` 가 **매 빌드에 경고**한다.

**고칠 것** — 뒤의 `color: p.color,` 를 지운다. `ini: p.ini,` 바로 뒤.

> 저장소(`app/src/App.jsx`)에서는 지웠다. **DC 마스터에서 지워야 안 돌아온다.**

---

## 11. `LOCATIONS[].gated` 가 아무도 읽지 않는 죽은 데이터 (2026-07-26 발견)

`LOCATIONS` 의 별채 행에 `gated: 's1'` 이 있는데 **참조하는 곳이 없다.**
실제 도면 게이트는 `buildFloor()` 가 `solved.s1` 을 직접 보고 판정했다.

**앱은 게이트 조건을 엔진에서 받게 바꿨다** — `floor_plan.buildings[].revealed_after`
→ `GEO.buildings[].gate` → `annexOn`. **어느 건물이 가려지는지는 여전히 필터가
`'annex'` 이름으로 찾는다**(6곳). 다른 건물을 가리는 사건이 오면 그때 데이터로 돌린다.

**고칠 것** — 프로토타입에서는 `gated` 를 지우거나 실제로 읽게 한다. 둘 중 하나여야
한다. 지금은 「있는데 안 쓰는 필드」라 다음 사람이 그걸 고치면 된다고 착각한다.

**같은 부류 하나 더** — `pb.axisLock` 도 선언만 있고 읽는 곳이 없다(60행).
이름으로 보아 드래그 축 고정을 하려던 자리다. 지우거나 구현한다.

---

## 12. 「관계 그래프」 → **「관계도」** (2026-07-26 사용자 결정)

기능 이름을 바꿨다. 한국어만 — 영문 `'Graph'` 는 그대로 둔다.

| 프로토타입 | App.jsx | 무엇 |
|---|---|---|
| 1301 | 74 | `navGraph: '관계 그래프'` → `'관계도'`. **사이드바와 화면 제목 둘 다 이 하나를 쓴다** |
| 2795 | 1903 | `'관계 그래프 · 알리바이 대조'` → `'관계도 · 알리바이 대조'` |
| 2802 | 1910 | `'관계 그래프 · 재구성'` → `'관계도 · 재구성'` |

> 뒤의 둘은 표기 안내(범례)다. 앞만 바꾸면 **범례가 없는 이름을 설명한다** —
> 9번이 지적한 것과 같은 종류의 거짓이 된다.

**저장소(`app/src/App.jsx`)에는 적용됐다. DC 마스터에는 아직이다.**

---

## 참고 — 슬롯·장소·진술 격자는 프로토타입이 이미 맞다

엔진의 4슬롯(`t0~t3`)·`locations`(`atLodge`)·`incident.scene`·presence/claim 도출
모델은 프로토타입의 `LOCATIONS`(`scene`·`offsite` 표식)·`CLAIMS`/`CLAIM_LOC` 와
이미 일치한다 (엔진 데이터를 프로토타입에서 역설계했으므로). 프로토타입 쪽 수정 불필요.
